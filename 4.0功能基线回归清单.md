# 神秘复苏模拟器 4.0 功能基线回归清单

## 使用口径

本清单用于判断“4.0 以来已经可玩的体验是否仍然存在”，不能用 SQL/CRUD 单项通过代替。发布版权威输入始终是 `src/神秘复苏模拟器发布版/神秘复苏模拟器发布版.png` 的 `chara` 与 `ccv3`；`src/神秘复苏模拟器/神秘复苏模拟器.json` 是陈旧旁路数据，不得作为验收基线。

- `[x] 静态`：已由仓库文件或自动脚本证明。
- `[ ] 真页`：必须在当前发布版角色卡实际激活后，用 SillyTavern 页面证明。
- `[ ] 低频 AI`：只有用户明确要求真实生成/写库时才执行；不得连续重放。
- 任一数量、版本、ref/cache、公共 API、消息计数或清理结果不符，发布阻断。

## v8.7.4 冻结证据

冻结时间：2026-07-10 CST。基线提交：`HEAD=origin/main=3181948e20d9f49b4e975d1e16d6249713095c0a`。

| 证据 | 冻结值 |
|---|---|
| 发布版本 | `8.7.4` |
| 项目 CDN ref | `7e52d45`，禁止 `@main` |
| cache | `phase164-4-0-final-baseline-6-28-p5-4-hotfix14-mvu-v874` |
| 权威发布 PNG | `src/神秘复苏模拟器发布版/神秘复苏模拟器发布版.png` |
| 发布 PNG SHA256 | `17642baea1895e7460f4f4f7a4c6657c9c8746703d404fd16d8c278419ee2fb6` |
| 发布 PNG 大小 | `7,761,894` bytes |
| 发布 YAML | `src/神秘复苏模拟器发布版/index.yaml` |
| 发布 YAML SHA256 | `b30e6172caac887cdacf80139cc67c95e07cce4f0f6876ba9479f324aac388fe` |
| 世界书 | 383 条；禁用 33 条；最大启用条目 5851 字符 |
| 正则 | 33 条 |
| Tavern Helper 脚本 | 8 个，全部启用 |
| 数据库 | 14 张表 |

脚本顺序必须保持：

1. `mvu`
2. `hotfix-generation-ended-listeners`
3. `变量结构`
4. `界面美化`
5. `固定状态栏`
6. `spv3.9.5·数据库`
7. `神秘复苏数据库前端`
8. `消息内面板`

`chara` 与 `ccv3` 必须分别满足：版本 `8.7.4`；7 个项目资源均为 `@7e52d45`；8 个脚本 cache 均为冻结值；33 正则；8 脚本及上述顺序；`localhost`、`127.0.0.1`、`@main` 均为 0。

## Phase 0 静态门禁

- [x] 静态：`node scripts/verify-mfrs-release-png.mjs --self-test`
- [x] 静态：`node scripts/verify-mfrs-release-png.mjs --expect-version 8.7.4 --expect-ref 7e52d45 --expect-cache phase164-4-0-final-baseline-6-28-p5-4-hotfix14-mvu-v874 "src/神秘复苏模拟器发布版/神秘复苏模拟器发布版.png"`
- [x] 静态：开发版 worldbook gate 为 `383/33/5851`。
- [x] 静态：发布版 worldbook gate 为 `383/33/5851`。
- [x] 静态：`verify-mfrs-mvu-hotfix-regressions.mjs` 通过。
- [x] 静态：`verify-mfrs-database-frontend-p3.mjs` 通过。
- [x] 静态：`verify-mfrs-archive-ui-regressions.mjs --stage baseline` 通过；后续 Phase 1-5 目标只在相应阶段显式启用。
- [x] 静态：`verify-output-cleaning-regressions.mjs` 复跑通过。首次复跑因独立 worktree 缺少 `yaml` 依赖而中断；复用本机已安装的同仓库依赖后通过，未把环境失败误记为业务失败。

## 当前真页探测状态

2026-07-10 18:58 CST 已只读连接 `http://127.0.0.1:9222`，页面为 `http://127.0.0.1:8000/`。当前激活角色是 `Science Worship 20260628`，不是神秘复苏；角色列表中可读到 `神秘复苏模拟器发布版` v8.7.4（index 4），但未切换角色，避免改变用户当前页面状态。

因此下面数据只证明 CDP 可用，不是 v8.7.4 神秘复苏运行态基线：当前页面消息 5（AI 3、用户 2）、资源 250；MFRS panel/wrapper/brand/style/fixed slots 均 0，`MFRS`、`MysteryDatabaseFrontend`、`MysteryMessagePanel` 均未挂载。CDP `Log.enable` 返回缓存日志 9 条，其中 8 条 error：6 条 `Permissions policy violation: unload`，2 条既有 vendor storage 404。由于目标卡未激活，不能把这些数量归因于 v8.7.4。

- [ ] 真页：激活/通过正式 UI 导入权威发布 PNG 后，重新冻结 Console ERROR/WARN 数量。
- [ ] 真页：重新冻结 Network 总数、项目资源数、404/失败数；ISSUE-006 的既有 404 单列，不冒充本轮新增。
- [ ] 真页：重新冻结 MFRS DOM/API 数量。
- [ ] 真页：采集 1440×900 桌面截图。
- [ ] 真页：采集 375×812 和 500×901 移动截图。
- [ ] 真页：截图确认无横向溢出、文字/Logo/固定控件遮挡和长文本截断。

## 导入与运行态

- [ ] 真页：通过 SillyTavern UI/官方导入路径导入权威发布 PNG；禁止直接覆盖运行中的角色 PNG 文件。
- [ ] 真页：当前角色名、avatar、`character_version=8.7.4` 与权威卡一致。
- [ ] 真页：运行态 worldbook 为 383 条、禁用 33 条、最大启用 5851。
- [ ] 真页：8 个脚本全部加载；项目资源使用 `@7e52d45` 与冻结 cache，无 `localhost`、`127.0.0.1`、`@main`。
- [ ] 真页：`window.MFRS`、`MysteryDatabaseFrontend`、`MysteryMessagePanel`、数据库 API 均存在且方法未减少。
- [ ] 真页：切角色、切聊天、刷新后三套 UI 不跨卡残留；返回神秘复苏后可恢复单例。

## 开局、MVU 与正文

- [ ] 真页：第 0 条 AI 开场白保留真实 `<sp_start>`/开局表单，未被协议清洗误删。
- [ ] 真页：填写开局表单只把设定写入 `#send_textarea`；不自动发送。
- [ ] 低频 AI：用户明确批准后发送一次开局设定，等待回复完成、MVU 写回、保存与清洗。
- [ ] 真页：`stat_data` 可从对应消息楼层读取；姓名、身份、位置、阶段、风险、灵异事件、厉鬼、在场人物不串楼层。
- [ ] 真页：只给 AI 消息注入正文 wrapper 和状态面板；用户消息注入数为 0；`mesid="0"` 的 AI 楼层不被误排除。
- [ ] 真页：三次 `refreshAll`/消息重渲染后，每个 AI 楼层最多 1 panel、1 wrapper、1 brand、1套样式；Tab、焦点和滚动位置不被无意义重置。
- [ ] 真页：正文可见层不残留 `<UpdateVariable>`、`<JSONPatch>`、`<stat_data>`、`<mfrs_*>` 等协议块。
- [ ] 真页：历史 AI 楼层持续动画暂停，最新 AI 楼层允许设计规定的持续动画；`prefers-reduced-motion` 下全部持续动画停止。

## 状态面板与行动建议

- [ ] 真页：状态/关系环境两个 Tab 内容完整，默认状态 Tab 激活，tabpanel/selected/controls 对应正确。
- [ ] 真页：鼠标点击、Tab 键、方向键、Home/End 均可切换，焦点可见。
- [ ] 真页：A/B/C/D 行动建议文案、主要风险、预期收益、死亡风险、复苏风险保持完整。
- [ ] 真页：点击行动建议只填充输入框并聚焦，不发送消息；测试后清空输入框。
- [ ] 真页：厉鬼按代号/名称去重，在场人物名称与描述显示正确，空状态不报错。

## 固定双插槽与档案柜

- [ ] 真页：`#mfrs-fixed-status-host` 仅维护 dashboard order 10 与 frontend order 20；旧 status order 30 UI 不回归。
- [ ] 真页：`.acu-embedded-dashboard-container` 与 `.acu-wrapper` 各最多 1，重复加载不增长。
- [ ] 真页：14 张表名称、数量、行数据、row_id 与 v8.7.4 基线一致。
- [ ] 真页：`MysteryDatabaseFrontend` 的公开方法集合不减少；`exportCurrentData()` 可独立读取真实落盘数据。
- [ ] 真页：档案柜 Tab、折叠入口、关闭入口均可聚焦，具备正确 ARIA，Enter/Space/方向键可操作。
- [ ] 真页：所有交互有 `:focus-visible`，触控目标不小于 44×44px，无无条件 outline 抑制。
- [ ] 真页：用户已有主题/折叠配置不丢失；默认主题迁移只执行一次且可回滚。

## 数据库与可逆写入

- [ ] 真页：先记录 `SP·数据库 III -> 高级工具 -> 运行日志` 最新时间戳，只判断之后新增日志。
- [ ] 真页：用 `previewTableChangePlan()` 预览一条带唯一 token 的测试变更，不直接调用内部缓存读法代替验证。
- [ ] 真页：用 `applyTableChangePlan()` 完成插入、更新、删除；每一步返回成功并刷新前端。
- [ ] 真页：用 `exportCurrentData()` 复查唯一 token 残留为 0。
- [ ] 真页：无新增 SQL/约束、provider、SQLite、`API_MUTATION_FAILED`、限流 ERROR/WARN。
- [ ] 低频 AI：仅用户明确要求时观察真实自动填表；触发前冻结行数，触发后复查落盘和清理。

## 发布资源与自动更新

- [ ] 真页/CDN：发布 YAML、权威 PNG、头像 PNG、7 个项目脚本、vendor 与数据库前端均 HTTP 200。
- [ ] 真页/CDN：加载资源 ref/cache 与冻结值一致，无旧 ref/cache、本地链接或 `@main`。
- [ ] 真页：自动更新提示只展示当前与远端版本差异；不点击“立即手动更新”，除非用户明确要求。
- [ ] 真页：只改 CDN 前端资源时不强迫重新导卡；卡本体变更时版本/YAML/chara/ccv3 同步。

## 完成定义

只有静态门禁全绿、目标发布卡真页 Console/Network/DOM 与桌面/移动截图齐全、核心 4.0 体验无退化、测试写入残留为 0，才能判定通过。任何真页待验证项不得用旧 v8.7.3 截图、旁路 JSON、源码推断或非目标角色页面代替。
