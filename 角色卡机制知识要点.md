# 角色卡机制知识要点（为魔禁卡制作准备）

> 依据：tavern_sync 官方 schema（`character.zh.json`）+ 神秘复苏模拟器卡实际配置。
> 用途：作为魔禁卡制作时的机制参考。

---

## 一、角色卡构成（index.yaml 顶层字段）

| 字段 | 作用 | 必填 |
|------|------|------|
| `头像` | 头像图片路径；null 则打包为 JSON | 可选 |
| `版本` / `作者` / `备注` | 元信息 | 可选 |
| `第一条消息` | 玩家开局看到的首条消息（`内容` 内嵌 或 `文件` 外链） | - |
| `角色描述` | 角色卡主体描述（入口定位） | - |
| `锚点` | 存放 YAML 锚点，便于 `<<: *锚点` 复用（不会被实际使用） | - |
| `世界书名称` | 世界书名；null 则与卡同名 | - |
| `条目` | **世界书条目数组**（核心），可含 `文件夹` 分组 | ✅ |
| `扩展字段` | 正则数组 + 酒馆助手（脚本库 + 变量） | - |

tavern_sync 打包时把 index.yaml 的这些字段映射为酒馆可导入的角色卡 JSON / PNG。

---

## 二、世界书激活策略（条目何时激活）

每条世界书条目必须有 `激活策略`，其核心是 `类型`：

### 类型（枚举，三者选一）
| 类型 | 语义 | 对应酒馆 | 特点 |
|------|------|----------|------|
| **蓝灯** | 常量 activation（**常驻激活**） | constant | 只要满足 启用 + 激活概率 即可，无关键字要求 |
| **绿灯** | 选择性 activation（**关键字触发**） | selective | 蓝灯条件 + 需在扫描文本中匹配到 `关键字` |
| **向量化** | 向量化 activation | vectorized | 一般不使用 |

> ⚠️ 用户常说的"红灯"在本 schema 里不存在。你实际会用到的是 **蓝灯（常驻）** 和 **绿灯（关键字）**。酒馆原生另有红牌/黄牌等，但 tavern_sync 只开放这 3 种。

### 相关字段
| 字段 | 说明 |
|------|------|
| `关键字` (数组) | 绿灯条目必须匹配到其中**任意一个**才能激活 |
| `次要关键字` | 可选。若设置，除匹配 `关键字` 外，还需按 `逻辑` 满足 `关键字`。逻辑枚举：`与任意/与所有/非所有/非任意` |
| `扫描深度` | 1=仅扫最后一楼，2=扫最后两楼… 或 `与全局设置相同` |
| `激活概率` (0-100) | 激活的百分比概率 |
| `递归` | 条目激活后是否可激活/被其他条目激活：`不可被其他条目激活`、`不可激活其他条目`、`延迟递归(n)` |

### 神秘复苏卡实际用法
```yaml
# 蓝灯（常驻）：变量列表/系统提示词/对话示例等
激活策略: { 类型: 蓝灯 }

# 绿灯（关键字触发）：规则类条目
激活策略: { 类型: 绿灯, 关键字: [调查, 线索, 案件推进, ...] }

# 绿灯 + 次要关键字（需 AND/OR 组合）
激活策略:
  类型: 绿灯
  关键字: [A]
  次要关键字:
    逻辑: 与任意
    关键字: [B, C]
```

---

## 三、世界书插入位置（激活后插到哪）

每条条目必须有 `插入位置`：

| `类型` | 语义 |
|--------|------|
| `角色定义之前` / `角色定义之后` | 插到 AI 角色定义前后 |
| `示例消息之前` / `示例消息之后` | 插到示例消息前后 |
| `作者注释之前` / `作者注释之后` | 插到作者注释前后 |
| `指定深度` | 插入到指定 `深度`，需要 `角色`（系统/AI/用户）+ `深度`(数字) |

`顺序`（数字）必填，决定同一深度下多条条目的排列次序。

### 神秘复苏卡实际用法（奠定魔禁卡的深度约定）
```yaml
# 重要契约类条目 → 深度 0（最前，最靠近系统提示词）
插入位置: { 类型: 指定深度, 角色: 系统, 深度: 0, 顺序: 14720 }

# 对话示例 → 深度 4
插入位置: { 类型: 指定深度, 角色: 系统, 深度: 4, 顺序: 14710 }

# 常规世界书规则 → 深度 4（YAML 锚点复用）
<<: *世界书常驻规则   # = 蓝灯 + 指定深度/系统/4/14700 + 递归禁
插入位置: { 类型: 指定深度, 角色: 系统, 深度: 4, 顺序: 14700 }
```

**深度优先级经验**（神秘复苏卡约定）：
- 深度 **0**：系统提示词、变量（MVP核心请求），最关键、最先
- 深度 **3-4**：对话示例、常驻规则
- 顺序号（14700/14710/14720）进一步细分层级

---

## 四、特殊效果（可选）

| 字段 | 说明 |
|------|------|
| `黏性` (n) | 激活后，之后 n 条消息内始终激活，无视激活策略/概率 |
| `冷却` (n) | 激活后，之后 n 条消息内不能再激活 |
| `延迟` (n) | 聊天至少 n 楼时才允许激活 |

用于控制某些条目的出现频率/时机（如战斗结果的临时规则）。

---

## 五、正则（扩展字段 → 正则）

正则用于**格式化 AI 输出**和**隐藏内部协议**。字段：

| 字段 | 说明 |
|------|------|
| `正则名称` / `id` | 标识（id 需唯一，门禁 G3 会校验） |
| `启用` | 布尔 |
| `查找表达式` | 正则（建议以 `/.../gi` 包裹） |
| `替换为` / `内容` / `文件` | 替换目标（`替换为` 已弃用，用 `内容` 或 `文件`） |
| `修剪掉` (数组) | 捕获组中要额外删除的子串 |
| `来源` (必填) | 应用到哪些消息：`用户输入`/`AI输出`/`快捷命令`/`世界信息`/`思维链` |
| `作用于` (必填) | `仅格式显示`（只改界面显示）/`仅格式提示词`（只改回传给AI的提示词） |
| `最小深度`/`最大深度` | 生效的楼层深度范围 |
| `在编辑时运行` | 编辑消息时是否运行 |

### 两个关键"作用于"方向
- **`仅格式显示: true`** → 只在玩家看到的界面上改，**不影响回传给 AI 的上下文**。（如隐藏 `<choices>`/`<UpdateVariable>` 等内部协议标签、隐藏草稿/调试摘要）
- **`仅格式提示词: true`** → 只改 AI 收到的提示词，不影响界面显示。

### 神秘复苏卡的正则分类（魔禁卡可沿用）
1. **`[显示]隐藏...`** 系列：`仅格式显示:true` + AI输出 → 隐藏内部协议块（`<choices>`/`<UpdateVariable>`/`<JSONPatch>`/`<sp_*>`/`<mfrs_*>`/草稿/英文调试摘要/裸 JSON），**从玩家界面隐藏但保留原始协议**（存 message.extra 供脚本读）
2. **`渲染...面板`** 系列：把 AI 输出的 `【状态面板】` 等标记替换成 HTML/CSS 面板 → 美化
3. **隐藏旧 sp/mfrs 文本面板**：白名单保留 `sp_start`/`sp_input`/`mfrs_roll`

> ⚠️ 正则 id 唯一性 + 查找表达式可编译由门禁 `verify-mfrs-regex-ids` 守护；正则与脚本清洗白名单需同步（防漂移）。

---

## 六、脚本库（扩展字段 → 酒馆助手 → 脚本库）

脚本库是运行时加载前端/脚本的清单。字段：

| 字段 | 说明 |
|------|------|
| `名称` / `id` / `启用` / `类型` | 项标识；`类型: 脚本` 或 `类型: 文件夹`(可嵌套) |
| `内容` / `文件` | 内嵌脚本内容 或 外链脚本路径（loader） |
| `介绍` | 脚本说明 |
| `按钮` / `数据` / `导出时携带` | 附加配置 |

### 加载方式（神秘复苏卡实际模式）
每个脚本项用一段 **loader 代码**动态 `import()` 编译产物：
```js
const loadModule = async (label, url) => { /* 重试20次 import(url+时间戳) */ };
loadLocalModule('变量结构', 'https://.../dist/神秘复苏模拟器/脚本/变量结构/index.js?v=版本_时间戳');
```
- 发布版指向 CDN（jsdelivr），开发版指向本地 `127.0.0.1:5510`
- URL 末尾加 `?v=` 版本 + `t=Date.now()` 防缓存

---

## 七、提示词构成（AI 行为来源）

| 组成 | 位置/深度 | 作用 |
|------|-----------|------|
| **系统提示词** | 深度 0（最前） | AI 的核心行为纲领：世界观铁律、变量档案规则、正文输出契约、格式优先级、核心任务、叙事风格、推理/死亡例外 |
| **对话示例** | 深度 4 | 给 AI 示范正确的输出格式（含协议块/折叠面板数据），Few-shot |
| **角色描述** | 顶层字段 | 卡的主体入口定位 |
| **第一条消息** | 顶层字段 | 玩家看到的开局内容（如 `<sp_start>` 触发开局面板） |
| **世界书** | 按激活插入 | 按需注入的设定知识（规则/地点/人物/势力等） |

**提示词排布原则**：系统提示词在最前（深度0）保证 AI 优先遵循；对话示例靠近以示范格式；世界书按需激活避免 token 浪费。

---

## 八、给魔禁卡的具体落地建议

1. **激活策略**：常驻铁律用**蓝灯**；规则类用**绿灯 + 关键字**（关键字要覆盖触发场景的红灯常用词）；变量条目用蓝灯深度0
2. **插入深度**：系统提示词/变量 → 深度 0；世界书常驻规则 → 深度 4（用 YAML 锚点 `<<: *世界书常驻规则` 复用）
3. **正则**（新建魔禁版）：
   - `[显示]隐藏...` 系列 + `仅格式显示:true` → 隐藏魔禁协议块（`[[MFrsStatus]]`、`<choices>` 等）
   - `渲染...折叠面板` 正则 → 把 `[[MFrsStatus]]玩家[[/MFrsStatus]]` 替换为 `<details>` 占位容器（数据由消息内面板脚本填）
   - 记得与脚本清洗白名单同步
4. **脚本库**：6 项（MVU / 变量结构 / mvu-protocol-applier / 消息内面板 / 固定状态栏 / 界面美化）+ loader 模式
5. **提示词**：系统提示词定义 AI 每轮输出 `<UpdateVariable>` 协议 + `[[MFrsStatus]]` 标记的规则；对话示例演示正确格式
6. **契约真源顺序**：schema.ts → 变量输出格式 → 系统提示词 → 对话示例 → 脚本解析
7. **门禁**：正则 id 唯一、脚本白名单、schema 与 initvar 一致（verify:mfrs-* 系列）

---
*完毕。以上机制已充分理解，可作为魔禁卡 step 2-8 的落地依据。*