# Phase 1 发现

## 当前输入

- 证据表：106 项。
- 世界书：82 个事件包、554 个节点。
- 当前欢迎页：39 个分组、87 个开场、1 个自定义入口。

## 已发现的旧映射问题

- `evidence-mapping.json` 中 `S08` 被映射到 `LEARNING-DEVICE`，但证据表语义为“美琴生日／黑子入住、9982诞生”。
- `O17` 被映射到 `BOOK-OF-LAW`，但证据表语义为“必要之恶教会特别编入考试”。
- `O32` 被映射到 `DRAGON`，但证据表语义为“暗部大战 Battle Royale”。
- `O34` 被映射到 `BRITISH-ROYAL`，但证据表语义为“后方之水”。
- `O35` 被映射到 `WW3`，但证据表语义为“英国王室／不列颠万圣节”。
- `O37` 被映射到 `DRAGON`，但证据表语义为“第三次世界大战”。

这些错误说明现有静态门禁只验证了 ID 完整性，没有验证语义一致性。

## 当前欢迎页风险

- 存在两个同名“暗部大战篇”分组。
- `新约开始·WWIII` 命名错误，三战属旧约结尾。
- 幻想御手、天使坠落、0930、C 文书、暗部大战、新约后期等存在日期或剧透风险。
- 现有 DOM 仍是“篇章组 → 开场”两级，不承载四级数据结构。

## Phase 1 结果

- 106 项证据全部完成映射，90 项有直接世界书包，1 项仅有关联包，15 项为 reference-only。
- 58 项证据已有当前欢迎页入口，48 项尚无当前欢迎页入口。
- 87 个现有开场全部完成候选映射，其中 43 个需要 Phase 2 精化，44 个当前为单包/单证据映射。
- 39 个现有分组全部完成候选映射，其中 13 个开场处于多包混组状态。
- 修正旧 evidence mapping 语义错位 9 处：S08、S09、S11、O17、O28、O32、O34、O35、O37。
- Y＋1 年 8 项证据（创约7–14）当前均无欢迎页入口。
- 未定日 6 项证据当前均无欢迎页入口。

## Phase 2 发现

### 分类体系

- 时间层：4 个，`TL-YMINUS1`、`TL-Y`、`TL-YPLUS1`、`TL-UNDATED`。
- 作品线：7 个，本篇、超电磁炮、一方通行、ITEM、心理掌握、其他正式外传、媒体／未定日衍生。
- 剧情阶段：10 个，旧约、新约、创约只作为本篇内部阶段，不与外传作品线并列。
- 章节规则：每个证据 ID 对应一个唯一 `CH-{EvidenceID}`，多槽位开场共享同一章节。
- 开场规则：每个目标开场使用 `OP-{EvidenceID}-{Slot}`，槽位从 `01` 开始。

### 数量与映射

- 106 项证据生成 106 个唯一章节 ID 与 114 个唯一目标 Opening ID。
- 87 个现有开场全部映射到目标 Opening，无丢失、无重复 Current ID。
- 52 个目标 Opening 已有当前入口，62 个目标 Opening 需要新增。
- 17 个目标 Opening 需要合并多个现有入口，覆盖 52 个现有开场；35 个目标 Opening 与现有开场一一对应。
- 50 个带世界书包的新目标需要新增；12 个无当前入口的 reference-only 目标需要新增。
- reference-only 目标共 15 个，其中 3 个已有当前入口，后续需要复核其世界书证据承载方式。

### 时间层与作品线分布

- 时间层：Y−1及更早 16 个；Y年 80 个；Y＋1年 12 个；未定日 6 个。
- 作品线：本篇 69 个；超电磁炮 25 个；ITEM 6 个；一方通行 3 个；其他正式外传 4 个；心理掌握 1 个；媒体／未定日衍生 6 个。

### 多槽位证据

- `P01` 才人工房：2 槽。
- `O01` 幻想御手：3 槽。
- `O11` 0930：2 槽。
- `G07` 创约第七卷：3 槽。
- `G10` 创约第十卷：2 槽。
- `G14` 创约第十四卷：2 槽。

### Phase 2 修正

- 初版校验把“多个开场共享同一章节 ID”误判为重复。已改为按唯一章节集合校验，并要求章节数量等于证据数量。
- 目标开场数量校验从范围检查改为精确 114 个。
- 增加章节 ID、时间层、作品线、剧情阶段引用校验。
- `O12` 保持 Phase 1 的修正结果，映射为 `KAZAKIRI`，不再归入 `ST0930`。

## Phase 3 发现

### 重排结果

- 87 个现有开场全部保留，未新增开场，未修改旧正文。
- 87 条旧入口覆盖 52 个 Phase 2 目标 Opening。
- 52 个目标 Opening 在欢迎页显示层归并为 51 个章节分组；差异来自幻想御手两个已有槽位共享同一章节。
- 39 个旧分组完成迁移审计。
- 排序结果：Y−1及更早前史 2 条；Y年 85 条。现有入口尚无 Y＋1年与未定日内容，需 Phase 5 补齐。

### 旧分组问题

- 15 个旧分组需要拆分。
- 5 个旧分组存在跨作品线混组。
- 2 个旧分组记录使用同名“暗部大战篇”，需要改名并拆分。
- 8 个新章节需要合并来自多个旧分组的入口。
- 43 个新章节为单旧分组改名后保留。

### 关键修复

- 两个“暗部大战篇”不再同名：
  - 旧分组 22 的 `O34` 改为 `魔法禁书目录本篇｜旧约·后方之水`。
  - 旧分组 25 拆为 `魔法禁书目录本篇｜旧约·暗部大战 Battle Royale` 与 `某科学的超电磁炮｜越狱`。
- `新约开始·WWIII` 拆分：
  - `O37` 改为 `魔法禁书目录本篇｜旧约·第三次世界大战`。
  - `N01` 改为 `魔法禁书目录本篇｜新约·新入生／黑夜海鸟、芙蕾梅亚`。
- 妹妹们事件拆分：
  - `S08` 归入 `某科学的超电磁炮｜美琴生日／黑子入住、9982诞生`。
  - `O08` 归入 `魔法禁书目录本篇｜旧约·妹妹调查／绝对能力者进化`。
- 大霸星祭拆分为本篇 `O23` 与超炮 `O24` 两个作品线视角。
- 天使坠落与最后之作拆分为 `O10`、`O11` 两个章节。
- `O12` 落入 `魔法禁书目录本篇｜旧约·新学期、风斩冰华与雪莉入侵`，确认不再误归 `ST0930`。

### Phase 3 产物

- 机器可读迁移表：`phase3-current-reorder.json`。
- 人工审阅表：`phase3-current-reorder.md`。
- 生成脚本：`phase3-build-reorder.mjs`。

## Phase 4 发现

### 写作分级

- 87 条现有开场全部完成剧透风险分级。
- 43 条需要改写，44 条当前危机明确且无后期揭露，先原样保留。
- 43 条改写中，36 条涉及剧透、身份揭露、跨篇总结、结局句或阶段边界错误。
- 12 条受欢迎页 `data-desc` 属性内嵌英文双引号影响，Phase 1 解析时被截断；Phase 4 已在规划稿中补全。
- 改写稿长度范围实际为 85–100 字，全部落在 80–160 字合同内。

### 重点修复

- 欧提努斯篇旧文本把世界重置、相互理解、放弃神枪与后续对抗一次写完，已改为蜜蚁篇的调查切口。
- 蜜蚁爱愉篇旧文本提前写出心理掌握被击沉、真相与错过，已改为记忆异常、装置调查与对抗前夜。
- 圣日耳曼篇旧文本直接引用终局台词，已改为钻石大楼封锁与出口、同伴联络危机。
- 僧正篇旧文本写出击败圣日耳曼、三大魔神现世与上里登场，已改为天台邀约、学生疏散与谈判切口。
- 元素篇旧文本揭露木原唯一伪装身份，已改为断电、热浪、避难与元素遭遇。
- 亚雷斯塔篇旧文本写出救援结果、右手归属与生死不明，已改为离城路线、追踪术式与解除代价。
- 科隆尊篇旧文本写出枪击、结盟、封印、赴英与旧时代落幕，已改为登陆分散、通信不稳与接触渠道。

### 旧约与中期修复

- 幻想御手 7/17、7/18 不再提前写出救援与功劳归属。
- 妹妹们 8/15、8/21 不再以死亡观察和实验中止作收束。
- 0930 不再提前写黑翼斩落木原数多，改为白天安置与夜间异动。
- 暗部大战、越狱、后方之水、英国王室、DRAGON、WWIII 的结局句均改为当前危机。
- `新约开始·WWIII` 的阶段边界错误已在正文中移除；WWIII 保留旧约，新约从新入生事件重新切入。

### Phase 4 修正记录

- 初版预估 41 条改写，实际复核后确定为 43 条；新增后方之水与 DRAGON 两条风险正文。
- 初版把 `CUR-0054` 与 `CUR-0067` 的篇章语义写反，已修正：
  - `CUR-0054` 归 0930。
  - `CUR-0067` 归幽幻姐妹。
  - `CUR-0056` 是暗部大战 10/8，当前稿无需改写，保留。
- 为满足正文合同，43 条改写稿统一增加 Phase 3 日期前缀。

### Phase 4 产物

- 机器可读写稿：`phase4-opening-rewrites.json`。
- 人工审阅稿：`phase4-opening-rewrites.md`。
- 生成脚本：`phase4-build-rewrites.mjs`。

## Phase 5 发现

### 完整清单

- 完整目标 Opening：114 个。
- 新增目标：62 个。
- 既有目标：52 个，来源覆盖当前 87 个开场。
- 17 个既有目标合并多个旧入口；Phase 5 为每个目标选定一条 canonical 正文，同时保留全部 Current ID 来源。
- 世界书包支撑：99 个；无包 reference-only：15 个。
- `O18` 的覆盖标记为 reference-only，但有 `ENDEYMION` 关联包，因此不计入“无包 reference-only”。

### 时间与作品线分布

- 时间层：Y−1及更早前史 16 个；Y年 80 个；Y＋1年 12 个；未定日 6 个。
- 作品线：本篇 69 个；超电磁炮 25 个；ITEM 6 个；一方通行 3 个；其他正式外传 4 个；心理掌握 1 个；媒体／未定日衍生 6 个。

### 新增覆盖

- Y−1前史：才人工房双槽、美琴一年级、暗黑五月背景、ITEM第一至第六卷代表入口、生物黑客、上蜂往事、绝对等速。
- Y年补口：幻想御手前奏、吸血杀手、八月三十一日三线并行、学艺都市、未元物质、必要之恶考试、Nectar、本篇大霸星祭、亚得里亚海女王、Skill-Out、独立心理掌握、新约终局、新约22R、创约第一至第六卷。
- Y＋1年：创约第七卷三日槽位、创约第八至第十四卷、创约10的 `1月6日23:58附近`、创约14的 `1月9日08:30附近`。
- 未定日：SS2／SP／特典散点、原创疑点、神裂SS、超炮SS3等外传、黄金SS、超出图版后续项。

### 正文归一化

- Phase 5 为 24 个 Phase 4 低风险保留但不足 80 字的既有目标补写 canonical 正文。
- 62 个新增目标全部满足 80–160 字、日期或相对时期前缀、当前危机与玩家介入点。
- 完整清单正文长度实际为 80–100 字。
- 未定日入口全部只显示“未定日”，不伪造具体日期。

### 边界确认

- 创约第一至第十四卷入口齐备。
- 未纳入创约15或更后内容。
- 15 个无包 reference-only 目标不伪造世界书节点。
- 本阶段仍未接入 MVU 初始化数据。

### Phase 5 产物

- 完整机器清单：`phase5-complete-openings.json`。
- 人工审阅清单：`phase5-complete-openings.md`。
- 生成脚本：`phase5-build-missing-openings.mjs`。

## Phase 6 发现

### 初始化模式

- 114 个目标 Opening 全部生成初始化记录，Opening ID 唯一。
- 98 个入口为 `node-chain`，可直接初始化事件包当前节点。
- 15 个入口为 `reference-only-no-package`，不携带伪起始节点、伪当前节点或伪进度。
- O18 为 `reference-only-related-package`：保留 `ENDEYMION` 关联，但当前节点仍为“未进入事件包”，避免把剧场版前传与游戏路线混成同一可玩链。
- 关联事件包总数为 99，与 Phase 5 统计口径一致。

### 多槽位节点裁决

- `OP-P01-02` 映射 `TALENT-WORKSHOP-05`，前一至四节点记为已完成。
- `OP-O01-02` 映射 `FANTASY-HAND-02`，`OP-O01-03` 映射 `FANTASY-HAND-03`；后者不宣称幻想御手全案结束。
- `OP-O11-02` 采用 `branching-multi-entry`：默认当前节点 `AUGUST-31-05`，扩展字段保留 `AUGUST-31-01`、`AUGUST-31-05`、`AUGUST-31-07` 三个可选入口；选择本槽不会自动完成最后之作线。
- `OP-G07-02` 映射 `GENESIS-07-02`，`OP-G07-03` 映射 `GENESIS-07-05`。
- `OP-G10-02` 映射 `GENESIS-10-07`。
- `OP-G14-01` 映射 `GENESIS-14-07`，`OP-G14-02` 映射 `GENESIS-14-08`，附尾 `GENESIS-14-09` 保留为未发生。
- 六个多槽位事件包的同包 Opening 当前节点均不相同。

### 兼容与扩展

- `compatibilityPayload` 只使用当前 schema 已存在字段。
- `剧情阶段` 不塞入旧约／新约／创约，而是按当前节点位置映射为遭遇／发展／高潮／终局。
- `所在位置` 统一为“待玩家确认具体位置”，不把正文模糊场景伪装成精确地点。
- `upcomingNodeIds` 表示尚未发生或尚未完成；分支入口也会保留在未完成集合中。
- 时间层、日期、时间段、作品线、篇章、事件包、尚未发生节点等新增字段全部放在 `extensionProposal`，状态为 `proposed-not-yet-supported`。

### Phase 6 产物

- 初始化数据：`phase6-initialization.json`。
- 人工审阅方案：`phase6-initialization.md`。
- Schema 扩展提案：`phase6-schema-extension-proposal.json`。
- 生成脚本：`phase6-build-initialization.mjs`。
- 多槽位检查脚本：`phase6-inspect-nodes.mjs`。

## Phase 7 发现

### 静态预检修正

- `OP-O28-01` 的正文出现两次“9月30日”日期前缀，Phase 7 已修正为单一前缀。
- `OP-O32-01` 误选英国政变入口，导致暗部大战证据被英国王室文本覆盖；已改回10月9日暗部大战／垣根帝督入口。
- `OP-O37-01` 误用10月3日旧入口；已改回10月19日俄罗斯宣战与第三次世界大战入口。
- Phase 6 节点解析正则在多行模式下让 `$` 提前匹配标题行末尾，导致大量当前节点只有标题、缺少进入条件与场景目标；已改为绝对结尾断言并重建 Phase 6。

### 数据结构与排序

- 数据层四级与欢迎页三级结构全部通过。
- Phase 7 生成独立显示模型：4 个时间层、106 个欢迎页分组、114 个 Opening。
- 原始 Phase 6 记录保留证据顺序；欢迎页显示模型按日期与层级重新排序，共 74 个位置发生变化。
- 分组排序采用“时间层 → 组内最早日期 → 作品线顺序 → 阶段顺序”，组内先按槽位再按日期。
- 连续精确日期倒序检查通过；未定日只保留在 `TL-UNDATED`。

### 归属、节点与剧透

- 时间层、作品线、剧情阶段、篇章与事件包归属全部可回溯到 Phase 1/2 与 worker manifests。
- 98 条 node-chain 的起始、当前、已完成与尚未发生节点全部存在，且节点覆盖完整。
- 15 条无包 reference-only 不携带伪节点；O18 保留 `ENDEYMION` 关联但仍为 reference-only。
- 9 个 Phase 1 语义修正锚点全部通过。
- 正文长度、日期前缀、介入点、禁用结局用语与安全字符全部通过。
- 兼容载荷字段与当前 schema 完全匹配；扩展字段仍为待升级提案。
- 未出现创约15。

### Phase 7 产物

- 静态验证数据：`phase7-static-validation.json`。
- 静态验证报告：`phase7-static-validation.md`。
- 生成脚本：`phase7-static-validation.mjs`。

## Phase 8 发现

### 接入策略

- 欢迎页 DOM 使用 Phase 7 `displayModel`：`mw-time-layer → mw-event-group → mw-scene`。
- 每个 `mw-scene` 同时携带 `data-opening-id`、`data-date`、`data-tag`、`data-desc`、`data-compat`、`data-meta` 与 `data-slot`。
- `data-compat` 只包含当前 schema 已支持字段，并在生成时合并到 `__mfrs_baseline`。
- `data-meta` 只作为模型可见元数据，用于生成【开局初始化】文本，不新增 MVU schema 字段。
- `开局地点` 与 `世界线记录` 使用现有 schema 字段承载当前日期、篇章和开场影响。
- 自定义开场不解析 `data-compat` / `data-meta`，保留原玩家档案基线与默认主线状态。

### 当前边界

- 源码接入与源内静态自检通过：4 层、106 组、114 个唯一 Opening。
- watcher 已成功刷新开发 dist，目标文件增量类型检查仅剩 3 个既有错误，未出现 Phase 8 新增类型错误。
- 当前 PNG 仍是旧版本 `1.0.0`；必须重新打包后才会包含 114 个新开场。
- `pnpm watch` 与 webpack watch 正在运行；按项目红线，不能执行会写同一 `dist` 的生产构建。
- Phase 8 打包校验器已准备完毕，但需等新 PNG 生成后运行。

### 打包结果

- 生产构建与显式打包均成功，最终 PNG 版本为 `1.1.0`。
- 最终 PNG SHA256：`1266AC301EE0F5C648BD9113549359CF3A0A069BF21D083BCBF9E7A89A9C6B44`。
- Phase 8 专用校验确认源欢迎页与 PNG 内欢迎页逐字一致。
- 114 个 `data-compat` 与 114 个 `data-meta` 均可 JSON 解析，并与 Phase 6 记录一致。
- 98 条 node-chain 涉及 541 个节点 ID，全部能在打包后世界书文本中找到。
- `verify-worldbook-pollution-gate` 的默认 5851 阈值失败为旧卡已存在的问题；旧卡有 3 个超长项，新卡为 2 个，未由 Phase 8 引入。
- 当前卡是开发卡，界面脚本仍从 `http://127.0.0.1:5510/dist/魔法禁书目录模拟器/脚本/界面美化/index.js` 加载，导入前需保持本机 5510 服务可用。

### 正则同步重打包发现

- 真实酒馆只读验证证明：世界书欢迎页已是新版，但 DOM 仍为 0 个时间层、39 个分组、87 个开场、0 个 `data-opening-id`。
- 根因不是世界书或运行时脚本，而是 `index.yaml` 中 `[显示]渲染魔法禁书目录开局页` 的 `替换为` 仍是旧 HTML。
- 旧正则的精确转换规则为：取源欢迎页 `<style>` 到 `</body>` 前的片段，去除文档外壳，逐行去缩进，并保留 `</style>` 后的空行。
- 使用该规则转换旧欢迎页后，与旧正则逐字符一致；因此新正则沿用同一规则，不把完整 HTML 文档标签注入消息 DOM。
- 新正则长度 371911，包含 4 个时间层、106 个分组、114 个唯一 Opening、114 个 `data-compat` 与 114 个 `data-meta`。
- Phase 8 校验器原只校验世界书，未校验 `extensions.regex_scripts`；本轮已补上显示正则源一致性、数量、启用状态、目标与剧透边界检查。
- CDN 只读检查确认存在可用更新：FontAwesome 6.x 最新 6.7.2、总最新 7.3.1；MagVar 0.183.0；tavern_resource 0.3.450；mfrs-img 最新提交 2d700a3。
- `mfrs-img` 当前提交与最新提交之间只改 README 与预览页，实际图片文件未变；本轮所有 CDN 锁定版本保持不变。
- 最终 `1.1.1` PNG SHA256 为 `3E97F4D7E808E3777F6B131B81DAABF3E0A66172930DFA2F6FBC3D8EAEB596A5`。

### 真机只读验收结论

- SillyTavern 当前选中角色为 `魔法禁书目录模拟器`，`characterId=10`，卡版本 `1.1.1`。
- 卡内世界书 114 条、正则 7 条；目标开局页正则与源欢迎页均为 4 层、106 组、114 个唯一 Opening。
- 实际 DOM 已出现 `custom-mw-time-layer`、`custom-mw-event-group` 与 114 个开场节点，说明正则同步后的新版页面已真实渲染。
- 114 组 `data-compat` 与 `data-meta` 均可解析，且每个开场节点都完整携带三类数据。
- 自定义开场控件保留，页面文本无 `创约15`。
- 运行时生产构建标记显示 `界面美化` built at `2026-09-08T11:24:42.811Z`。
- 结论：**Phase 8 的欢迎页正则同步、世界书数据、初始化载荷和自定义开场功能均已成功生效**。

### `1.1.2` CDN 分发验收结论

- 固定 CDN 提交：`ba1ff77c9abbf140ff622bab41721b6dcc6d7365`。
- 最终 PNG：7,320,821 bytes，SHA256 `708204B857813F7F26FC4194434999FFA320D93C2DB06E5C1FBE0D818FAD07AC`。
- `chara` / `ccv3` 双 payload 均为版本 `1.1.2`，本地 `5510` 残留为 0，固定 CDN 引用为 6。
- 真机资源记录显示 6 个固定 CDN 脚本全部 HTTP `200`。
- 实际 DOM 使用 `data-time-layer` / `data-event` / `data-opening-id` 表达层级与分组：4 / 106 / 114，兼容与元数据载荷各 114。
- `Mvu` 全局存在，包含 `getMvuData`、`replaceMvuData`、`parseMessage`、`getCurrentMvuData` 等运行时方法。
- 全局 TavernSync websocket 重连错误来自宿主扩展，不属于魔禁卡脚本；不阻塞 `1.1.2` 分发。
