# 魔禁开场白重整：Phase 8 打包与验收

## 目标

完成 114 个开场的新欢迎页、显示正则与初始化数据打包，输出《魔法禁书目录模拟器》`1.1.2` CDN 分发 PNG，并保留可回滚的修复记录。

## 边界

- 本阶段修改欢迎页源码、`index.yaml` 开局页显示正则、`界面美化/index.ts`、打包校验器与规划产物。
- 不修改 `schema.json`、`initvar.yaml`、MVU 机制或 loader 结构。
- 不把 CDN 依赖升级混入本次热修。
- 不纳入创约15或更后内容。
- 离线静态验证不宣称为真实 SillyTavern 运行时验收。

## 当前结果

- 数据层保持四级，欢迎页显示层保持三级。
- 世界书欢迎页：4 个时间层、106 个分组、114 个 Opening。
- 卡内显示正则：4 个时间层、106 个分组、114 个 Opening，且与源欢迎页转换结果逐字一致。
- 卡片版本：`1.1.2`。
- 产物：`src/魔法禁书目录模拟器/魔法禁书目录模拟器.png`。
- SHA256：`708204B857813F7F26FC4194434999FFA320D93C2DB06E5C1FBE0D818FAD07AC`。
- 自定义开场保留；创约15不存在。
- 98 条 node-chain 的 541 个必需节点 ID 全部存在于打包后世界书。

## 阶段

1. [complete] 生成 4 个时间层、106 个分组、114 个 Opening 的欢迎页 DOM。
2. [complete] 在 `界面美化/index.ts` 接入 `data-compat` 与 `data-meta`，并合并到现有 MVU baseline。
3. [complete] 保留自定义开场按钮、输入框与原生成流程。
4. [complete] 初次打包将卡片版本提升为 `1.1.0`；正则同步修复后提升为 `1.1.1`。
5. [complete] 备份 Phase 8 前 PNG 与正则同步前 `index.yaml` / PNG。
6. [complete] 添加并扩展 `phase8-package-validation.mjs`，同时校验世界书与显示正则。
7. [complete] 停止现有 watch 后执行一次性生产构建、打包 PNG 与静态验证。
8. [complete] 输出 Phase 8 初次打包报告并等待用户手动导入真实 SillyTavern 验收。
9. [complete] 只读检查 CDN 更新，保留现有锁定版本，避免与热修混入依赖升级。
10. [complete] 同步 `[显示]渲染魔法禁书目录开局页` 的 `替换为` 为最新欢迎页转换片段。
11. [complete] 扩展打包校验器，同时校验卡内世界书与卡内显示正则。
12. [complete] 重新构建、打包 `1.1.1`，通过离线静态门禁并输出最终报告。
13. [complete] 用户手动导入真实 SillyTavern，完成宿主运行时验收：版本 `1.1.1`、4 层、106 组、114 个唯一 Opening、114 组兼容/元数据载荷、自定义开场保留、无创约15。
14. [complete] 快进同步远端 bot 构建提交 `ba1ff77c`，并将 6 个脚本加载地址固定到该 CDN 提交。
15. [complete] 重新构建、打包 `1.1.2`，确认 PNG 双 payload 中本地 `5510` 残留为 0、固定 CDN 引用为 6。
16. [complete] 用户手动导入 `1.1.2`，完成真机只读验收：6 个 CDN 脚本 HTTP 200、欢迎页 4 层 / 106 组 / 114 Opening、MVU 全局存在。

## 下一步

`1.1.2` CDN 分发版已通过离线静态验证与真实 SillyTavern 只读验收。后续可选：单独执行一次“选择开场→初始化→生成”交互回归；如升级外部依赖，另开 `1.1.3`。
