# 已废弃：8.13 / MFRS 本地验收流程

本目录保留 8.13 阶段的历史测试产物，但旧 MFRS 验收命令不再可执行。

当前唯一开发与验收入口：

- `PROJECT_FLOW.md`：运行、验证、发布、端口职责与 Git 同步规则
- `docs/SIMPLIFIED_WORKFLOW.md`：面向使用者的日常操作手册

当前流程为：F5 → 切换开发模式 → `pnpm watch` → 固定 5510 静态服务 → 在 SillyTavern 8000 真页验收 → 切回生产模式。

本目录中的 β PNG 和旧测试材料仅用于历史对照，不应作为当前开发卡或发布卡使用。
