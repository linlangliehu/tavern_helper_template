# 路径决策（α 历史 + β 升格）

**最后更新**：2026-07-12（β0 冻结）  
**权威方案**：`设计方案-第四版.md`  
**任务清单**：`TASKLIST_BETA.md`

---

## A. 路径 α（已完成 · 8.11.0）

第一期消息内仿三栏，**已发版**；β 全屏 ON 时 hide/dismantle，**退出沉浸 / 非全屏时作回退**。

```
┌──────────────┬────────────────────────────┬────────┐
│ 现场档案     │ 坐标·阶段·事件·危害        │ 导航   │
│ (可折叠)     │────────────────────────────│ 正文   │
│ 身份/风险/   │     AI 叙事正文            │ 档案   │
│ 事件/厉鬼…   │     拟办意见 A/B/C/D       │ 关系   │
│              │                            │ 柜     │
└──────────────┴────────────────────────────┴────────┘
```

| 决策 | 选择 | 理由 |
|------|------|------|
| 布局落地 | **路径 α**：最新 AI 消息内 CSS Grid 三栏 | 不改 ST 全局；随卡发版；风险可控 |
| 固定 host | Phase3 **仅换皮** | 保留 `insertBefore` + order 10/20 |
| Brand | **删除**眼/阵 +「鬼眼封案」 | 对齐参考图 |
| 顶条 | 位置·阶段·事件代号·鬼域·危害 | 只读 `stat_data` |
| 行动 | 「拟办意见」；**只填不发** | 契约不变 |
| 路径 β（当时） | 延期 | 已由本节 B 升格 |

---

## B. 路径 β（现行主路径 · 建议 8.12.0）

| 决策 | 代号 / 选择 | 说明 |
|------|-------------|------|
| 主路径 | **β 全屏卷宗 HUD** | `#mfrs-hud-shell` fixed 全屏；直播 reparent `#chat` |
| 默认进入 | **D1 自动全屏** | 神秘复苏卡激活后自动 mount；壳内「退出沉浸」+ 可选 `Ctrl+Shift+G` toggle |
| 档案柜 | **C1 仅「柜」展开** | 覆盖层/抽屉；**禁止**全屏 ON 时默认半屏柜 |
| 验收节奏 | **先本地 dist** | `pnpm build` + `127.0.0.1:5500/dist/...`；通过后再批 8.12.0 |
| 模块落点 | **消息内面板** bundle 内 | **不**新增脚本库第 9 项；脚本库仍 8 序 |
| α 角色 | 回退 | 全屏 OFF / 退出沉浸后恢复 8.11 α 行为 |
| 固定 host | 不删节点 | 全屏 ON 视觉隐藏；挂载/id/order **10/20** 不变 |
| 数据 | 只读现有字段 | 不增生命/饱食/理智；拟办只填不发 |

### 用户确认（β0 · 2026-07-12）

- [x] **D1** 自动全屏（否决 D2 默认手动）  
- [x] **C1** 仅导航「柜」展开（C2 钉底栏延期）  
- [x] **本地 dist 验收优先**（否决未测直接 8.12 发布）  

### 本地验收 URL 模板（T0.5）

```
http://127.0.0.1:5500/dist/神秘复苏模拟器/脚本/<模块>/index.js
```

例：`…/脚本/消息内面板/index.js`  
开发卡可临时将脚本库指本地；**禁止**把 localhost 链写进发布版 / `publish-card` 产物。

---

## C. 不做清单（α+β 共同）

- 不换整个 SillyTavern 皮肤  
- 不把生命/饱食/理智写入 MVU；不造时间/天气字段  
- 不改正则 33 / 世界书 / SQL 业务 / 脚本库顺序·名称·启用  
- 不整包移植途尽正则巨页或美化 2.0 全文  
- 不手改发布版 PNG 作主路径  
- 未过本地 dist + 用户批准前不改 `CDN_REF` 发 8.12.0  
