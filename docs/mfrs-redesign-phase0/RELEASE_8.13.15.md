# 神秘复苏模拟器 8.13.15

**日期**：2026-07-13  
**主题**：BF0 — initvar/schema 与行动建议契约对齐

## 变更

- **C1/L7/M6**：`initvar` 根级 `规律推理记录`/`最近行动判定`/`行动建议`/`在场人物`；姓名/开局地点默认 `''`；补 `剧情阶段`、scene flags、`is_dead`、`可见档案`、主线权限键
- **C2**：schema 扩行动建议死亡/复苏风险枚举、判定触发项/资源代价/后续建议、推理确认等级；重生 `schema.json`
- **H1/D1/M11/H3**：存活时 `/行动建议` 恰 A–D 四条；死亡写集统一（状态/`is_dead`/阶段状态=模拟结束/行动建议=[]）；系统提示骨架对齐变量输出格式
- production dist 含 hotfix seed 扩展字段与变量结构

## 发布

| 项 | 值 |
|----|-----|
| 版本 | **8.13.15** |
| 功能源 commit | `5eaa533` |
| dist commit / CDN_REF | `107b3ff8aff3` |
| cache | `…-v81315-bf0-initvar-schema` |

## 使用

**请重导 8.13.15**。新开局根路径应有 `/行动建议`；模型按扩展字段写风险枚举时不应被 schema 剥掉。
