# Findings

## 项目运行流程常驻参考

### 1. 目录和职责

- 开发版角色卡目录：`src/神秘复苏模拟器/`。这里是主要编辑源，包括开发版 `index.yaml`、世界书、数据库、第一条消息、系统提示词、脚本源码和头像。
- 发布版角色卡目录：`src/神秘复苏模拟器发布版/`。这里由发布脚本从开发版镜像生成，并额外保留发布版版本号与 CDN 链接。
- 构建产物目录：`dist/神秘复苏模拟器/`。`npm run build` 生成脚本/界面产物，发布版 CDN 会指向这里的资源。
- 发布脚本：`scripts/publish-card.mjs`。负责镜像开发版到发布版、替换 localhost 链接为 CDN、保留 `版本: '3.0'`、调用 `tavern_sync.mjs bundle` 打包发布版 PNG。
- 角色卡打包脚本：`tavern_sync.mjs`。由 `npm run build` 或 `publish-card` 调用，将配置、世界书和脚本信息写入 PNG 的 `chara/ccv3` 元数据。

### 2. 开局页运行链路

- 实际首屏开局入口在 `src/神秘复苏模拟器/index.yaml`，关键条目是 `[显示]渲染神秘复苏开局页`。不要只改 `src/神秘复苏模拟器/世界书/自定义开局/欢迎页.txt`，否则会出现“源欢迎页变了，实际首屏没变”的错位。
- `index.yaml` 的首屏入口使用静态 `<select data-mfrs=anchor>` 承载开局节点。当前真实节点为 55 个，另有 1 个占位 option。
- 每个真实节点 value 必须保持七字段：`name|time|loc|phase|pressure|intel|boundary`。`time` 和 `loc` 是节点自带信息，不新增可见手填框。
- 每个真实节点应带 `data-group/data-chapter/data-name/data-time/data-loc`，供增强脚本分组和显示。
- `src/神秘复苏模拟器/脚本/界面美化/index.ts` 运行在 SillyTavern 宿主页，构建后由 `dist/神秘复苏模拟器/脚本/界面美化/index.js` 注入。它会把静态 select 增强为三级 `.mfrs-dropdown`：大类 -> 中间组 -> 事件。
- 点击提交时，界面美化脚本读取节点七字段，生成“神秘复苏·开局设定”文本并写入 `#send_textarea`。提交文本需包含剧情节点、当前时间、当前地点、原著阶段、事件压力、玩家可见情报、禁止泄露边界。

### 3. 开发构建流程

- 常用构建命令：`npm run build`。
- 构建会运行 webpack，生成 `dist/神秘复苏模拟器/**`，并通过 `tavern_sync` 重新打包开发版 PNG/相关配置。
- Windows/当前沙盒下 `npm run build` 可能在沙盒内报已知 `spawn EPERM`。这不是业务代码错误；需要按权限流程在沙盒外重跑同一命令。
- 构建后可检查 `dist/神秘复苏模拟器/脚本/界面美化/index.js` 是否包含 `mfrs-dropdown`、`querySelectorAll('option')` 和“当前时间”相关逻辑。

### 4. 发布版同步和打包流程

- 先确认开发版源码、dist 和开发版 PNG 已构建并验证。
- 如果发布版需要引用新版 CDN 脚本，必须采用两段式发布：
  - 第一段：提交并推送包含新版 `dist` 的资源提交。
  - 第二段：把 `scripts/publish-card.mjs` 中的 `CDN_REF` 更新为第一段资源提交完整 hash，把 `CDN_CACHE_VERSION` 更新为新的唯一值。
  - 然后运行 `npm run publish-card -- 神秘复苏模拟器发布版`，生成发布版 `index.yaml` 和发布版 PNG。
  - 最后提交并推送发布版 `index.yaml`、发布版 PNG、`scripts/publish-card.mjs`。
- 可先 dry-run：`npm run publish-card -- 神秘复苏模拟器发布版 --dry-run`。正常应镜像 5 个目录、约 386 个世界书文件、替换 6 处链接并保留版本 3.0。
- 正式发布：`npm run publish-card -- 神秘复苏模拟器发布版`。成功后会提示已打包到 `src/神秘复苏模拟器发布版/神秘复苏模拟器发布版.png`。
- 发布版必须保留 `版本: '3.0'`。`scripts/publish-card.mjs` 中的 `releaseVersion: '3.0'` 是防止开发版版本覆盖发布版版本的关键设置。

### 5. 验证流程

- 基础检查：`git diff --check`。
- 构建检查：`npm run build`；若沙盒内 `spawn EPERM`，按权限流程沙盒外重跑。
- 发布版 YAML 检查：`src/神秘复苏模拟器发布版/index.yaml` 应包含 `版本: '3.0'`、当前 `CDN_REF`、当前 `CDN_CACHE_VERSION`、强外挂文案和后期/番外节点关键字。
- PNG 元数据检查：PNG 的 `tEXt` 块里 `chara` 与 `ccv3` 是 base64 JSON，不能直接明文搜索；需先解码，再检查版本、CDN hash/cache、`当前时间`、`mfrs-dropdown`、强外挂文案和节点关键字。
- 浏览器/真实页检查：可用 Chrome DevTools Protocol。真实 SillyTavern 页面在 `http://127.0.0.1:8000/`，CDP 端口通常为 `127.0.0.1:9222`。刷新后若停在最近聊天列表，需要点回具体聊天后再检查欢迎页 root。
- 真实页通过标准：1 个欢迎页 root、1 个 dropdown、6 大类、17 中间组、55 事件项；早期节点和后期/番外节点都能写入七字段开局文本；390px 视口无横向溢出；本轮相关运行时异常为 0。

### 6. 常见坑

- 不要只改 `世界书/自定义开局/欢迎页.txt`。实际首屏入口还在 `index.yaml`。
- 不要新增可见时间/地点输入框。`time`/`loc` 来自开局节点自身。
- 不要让发布版直接引用尚未推送的 dist。发布版 CDN 必须指向已经存在于远端的资源提交。
- PNG 元数据检查必须解码 `chara/ccv3` base64 JSON。
- 构建可能回写无关 dist 文件，提交前用 `git diff --name-only` 和 `git diff --stat` 确认提交范围。
- `npx tsc --noEmit` 目前仍有既有全项目问题：`LiteralUnion`、Web Bluetooth 类型、未使用 `z`。不要把它误判成本轮开局/发布改动引入的问题。

## 最终公开发布发现（2026-06-02）

- 本轮最终公开发布采用两段式链路：先推送包含新版 dist 的资源提交 `d7bc106b54cbd66542d1c8265537cdad00eb8096`，再让发布版 CDN 指向该资源提交，避免发布版 PNG 引用尚未存在或仍为旧逻辑的 CDN 脚本。
- 推送第二段发布前，远端已有 `44e0c1b [bot] bundle` 位于 `d7bc106` 之后；本地已先 `git merge --ff-only origin/main` 快进到远端最新，再创建发布提交，避免基于旧 HEAD 推送。
- `scripts/publish-card.mjs` 当前发布配置：`CDN_REF = d7bc106b54cbd66542d1c8265537cdad00eb8096`，`CDN_CACHE_VERSION = phase106-start-anchor-runtime-fix-3-0`。
- `npm run publish-card -- 神秘复苏模拟器发布版 --dry-run` 与正式发布均通过；正式发布重新生成 `src/神秘复苏模拟器发布版/神秘复苏模拟器发布版.png`，发布版 `index.yaml` 的 6 处本地链接已替换为新 CDN hash/cache，并保留 `版本: '3.0'`。
- 发布版 PNG 不能直接按明文搜索 tEXt chunk；`chara` 与 `ccv3` 是 base64 JSON。解码后确认两个元数据块均包含版本 `3.0`、新 CDN hash/cache、「当前时间」、`mfrs-dropdown`、强外挂文案和「第四代与阴阳路·旧时代回声」。
- `npm run build` 在沙盒内仍因已知 `spawn EPERM` 失败，沙盒外重跑成功。构建会把 `dist/神秘复苏模拟器/界面/状态栏/index.html` 回写到 `d7bc106` 的资源版本；该文件是无关构建副作用，已恢复到当前 HEAD，未纳入 `2c18506` 发布提交。
- 第二段发布提交 `2c185067957440b60daac551fccc4b178ea385ae chore: publish mystery revival start anchors` 已推送；推送后 `HEAD` 与 `origin/main` 同步，未观察到新的 bot bundle 提交。

## 真实 SillyTavern 复测与异常修复发现（2026-06-02）

- 真实页环境：Chrome CDP `127.0.0.1:9222`，SillyTavern 页面 `http://127.0.0.1:8000/`。刷新后页面可能停在最近聊天列表，此时没有欢迎页 root；需点回最近聊天中的 `神秘复苏模拟器 – 神秘复苏模拟器 - 2026-06-01@14h30m40s627ms` 后，欢迎页 root 与发送框才恢复。
- 旧异常表现：真实页曾反复抛 `Uncaught TypeError: undefined is not iterable (cannot read property Symbol(Symbol.iterator))`，栈指向 `enhanceWelcomeAnchors` 附近。虽然菜单功能一度可用，但 MutationObserver/定时增强会重复触发异常，不能视为发布前完成。
- 追加修复策略：在已修复 `inWelcomeRoots(childSelector)` 的基础上，`enhanceWelcomeAnchors()` 进一步改为防御式 DOM 读取：
  - `querySelectorAll<Element>(...)` 后先用 `isHostSelectElement()` 校验 `tagName === 'SELECT'` 且属于宿主 `HTMLSelectElement` 或至少具备 `options` 属性。
  - 选项列表改用 `select.querySelectorAll<HTMLOptionElement>('option')`，不再使用 `Array.from(select.options)`。
  - 初始显示文本改用已收集的 `optionElements.find(option => option.selected && option.value.trim())`，不再依赖 `select.selectedOptions`。
  - `closeSiblingAccordions()` 改为遍历直接子元素并用 `matches(selector)` 判断，避免 `:scope` 在宿主页复杂上下文里的兼容风险。
- 构建后 dist 复核：`dist/神秘复苏模拟器/脚本/界面美化/index.js` 可检索到 `querySelectorAll('option')`，不再命中 `select.options`/`selectedOptions`。
- 真实页复测结果：点回具体聊天后，欢迎页增强为 1 个 dropdown、6 大类、17 中间组、55 事件项；选择「七中当日·周正讲课」和「第四代与阴阳路·旧时代回声」均写入 7 字段 value，并生成含「当前时间/当前地点/禁止泄露边界」的开局文本；发送框已恢复原值，未发送消息。
- 390px 视口下 body/document scrollWidth 均为 390，欢迎页内 overflowCount=0；本轮 CDP 捕获运行时异常 total=0、相关异常=0。控制台残留的数据库模板、固定状态栏和宏弃用日志与欢迎页增强修复无关。
- 发布提示：真实页已证明“本地最新 dist + 当前聊天环境”可用；公开发布仍取决于后续提交/推送新版 dist、更新 `CDN_REF`/`CDN_CACHE_VERSION` 并重新打包发布版 PNG。

## 发布版同步发现（2026-06-02）

- `scripts/publish-card.mjs` 原本会直接把开发版 `index.yaml` 复制到发布版，导致发布版顶层 `版本: '3.0'` 有被开发版 `版本: '2.0'` 覆盖的风险。本轮已为发布版配置增加 `releaseVersion: '3.0'`，同步 YAML 后强制保留发布版版本号。
- `npm run publish-card -- 神秘复苏模拟器发布版 --no-bundle` 可以正确镜像开发版目录到发布版，并同步 `index.yaml`、头像和 386 个世界书文件。
- 发布版 `index.yaml` 已同步为新版开局入口：版本为 `3.0`；55 个真实开局节点；每个真实节点为 `name|time|loc|phase|pressure|intel|boundary` 7 字段；每个真实节点带 `data-group/data-chapter/data-name/data-time/data-loc`；`mfrs-submit` 无旧内联 `onclick`；特殊能力已收紧为“强外挂模式（非默认）·永久死机驾驭”。
- 发布版 `世界书/自定义开局/欢迎页.txt` 已同步为新版 `rawAnchors -> anchors` 三级结构，并包含“当前时间”、`mfrs-dropdown` 和强外挂文案。
- 发布版 PNG 的 `chara/ccv3` 元数据已包含新版入口与版本 3.0，说明构建/打包层已写入发布版产物。
- 仍需注意：当前发布版仍引用旧 CDN `c41b53fbe2bd69788cb7ead88c28f6a6a7d7b1ff` 与 cache `phase105-theme-lighten-3-0`。因为新版提交逻辑依赖外部 `界面美化/index.js`，最终公开发布必须先产生并推送包含新版 dist 的资源提交，再更新 CDN_REF/cache 并重新打包。否则公开环境可能加载旧脚本，无法处理新版无内联 `onclick` 的开局按钮。
- 当前工具环境未暴露 Chrome DevTools MCP；`agent-browser` 不可用；`127.0.0.1:9333` 无活动 CDP 端口。本轮改用原生 CDP 启动独立 headless Chrome 监听 `127.0.0.1:9444`，加载发布版 `index.yaml` 欢迎页片段并注入本地最新 dist `界面美化/index.js` 做浏览器复测。
- 发布版本地 CDP 复测通过：增强后为 6 个大类、17 个中间组、55 个事件项；早期节点提交含正确“当前时间/当前地点”；带 viewport 的移动端约 413px 宽度无横向溢出，root/dropdown/menu 宽度约 366/326/326px；运行时错误与 CDP exception 均为 0。该复测验证的是“发布版源码 + 本地新版 dist”，不等同于公开 CDN 链路已经完成。

## Chrome/CDP 欢迎页验收发现（2026-06-02）

- 真实 Chrome/CDP 注入 dist 脚本时发现一个运行时 bug：样式成功注入，但三级菜单没有生成；`Runtime.exceptionThrown` 报 `TypeError: undefined is not iterable ... Array.from`。
- 根因是组合选择器写法错误：`welcomeRootSelector = '#mfrs-welcome-root, .mfrs-welcome-root, .custom-mfrs-welcome-root'`，直接拼成 ``${welcomeRootSelector} select[data-mfrs="anchor"]`` 时，CSS 会把 `#mfrs-welcome-root` 根节点也作为一个 selector 分支匹配进来，而根节点没有 `options` 属性。
- 修复方式：新增 `inWelcomeRoots(childSelector)`，把逗号分隔的根选择器逐个加上子选择器；`enhanceWelcomeAnchors()` 改为查询 `inWelcomeRoots('select[data-mfrs="anchor"]')`。
- 修复后 CDP 验收通过：1 个增强 dropdown、6 个大类、17 个中间组、55 个事件项；早期节点和后期/番外节点均能写回 7 字段 value，并生成含「当前时间/当前地点/原著阶段/事件压力/玩家可见情报/禁止泄露边界」的开局文本。
- 桌面 1365 宽和移动端 390 宽布局均无横向溢出；移动端菜单展开时 dropdown/menu 宽约 310px，未越界；CDP 运行时异常列表为空。

## 开局入口链路验证发现（2026-06-02 续接收尾）

- 实际首屏欢迎页由 `src/神秘复苏模拟器/index.yaml` 中的 `[显示]渲染神秘复苏开局页` 正则渲染；`src/神秘复苏模拟器/世界书/自定义开局/欢迎页.txt` 仍是独立欢迎页源/参考形态。以后修改开局入口时必须同步检查这两处，否则会出现“源欢迎页已改、实际首屏仍旧”的错位。
- `index.yaml` 的实际入口当前是静态 `<select data-mfrs="anchor">`：56 个 option 中 55 个真实开局节点，真实节点全部带 `data-group/data-chapter/data-name/data-time/data-loc`，value 全部为 7 字段 `name|time|loc|phase|pressure|intel|boundary`。
- `世界书/自定义开局/欢迎页.txt` 当前不是静态 select，而是 `rawAnchors` 三层数据 + JS 生成 `.mfrs-dropdown` 菜单；它同样已把 `time` 放到隐藏值第二位，并在提交文本中写「当前时间」。
- PNG 元数据解析需用无符号大端读取 chunk 长度（例如 Node `readUInt32BE`）。此前用有符号位移会导致 chunk offset 错乱并输出乱码。用正确解析后，开发版 PNG 的 `chara/ccv3` 均能解出，且包含 `当前时间`、`强外挂模式（非默认）·永久死机驾驭`、`mfrs-dropdown`、`第四代与阴阳路·旧时代回声`。
- `src/神秘复苏模拟器/神秘复苏模拟器.json` 时间戳早于本轮修改，不宜作为本轮入口链路验证对象；当前以 `dist/神秘复苏模拟器/脚本/界面美化/index.js` 与 `src/神秘复苏模拟器/神秘复苏模拟器.png` 为准。

## 三级开局节点数据集设计（2026-06-02 任务#1 交付）

结构：`anchors[{group 大类, groups[{title 中间组, items[{name,time,loc,phase,pressure,intel,boundary}]}]}]`（三级）。
`time` 用"剧情相对时间"标签（原著时间线模糊，不用精确日期）：灵异复苏初期 / 大昌市早期 / 总部体系建立期 / 大昌市灾害期 / 中期·势力博弈期 / 中期·规则地点活跃期 / 后期·队长计划期 / 后期·国际冲突阶段 / 终局前夕 / 终局后·后日谈 / 番外·时间错位。
`time` 与 `loc` 同属"节点自带、不手填"，仅拼进隐藏值并进开局文本"当前时间"行（与联动规则第4/12-14行一致）。

5 大类 + 自定义，共 55 个节点：

- **A 普通人视角（14）**
  - A1 七中与早期怪谈(5)：论坛鬼故事·雷电法王发帖 / 七中前数日·怪谈传播 / 七中当日·周正讲课 / 鬼域初现·校园迷路 / 鬼眼复苏·幸存余波
  - A2 大昌市街头(3)：商场购物·无头鬼影传闻 / 公寓邻里·敲门声扩散 / 出租屋夜归·跟随脚步
  - A3 灾害封锁区(3)：封锁区居民·避难所求生 / 积水街区·夜间撤离 / 避难医院·物资短缺
  - A4 规则地点误入(3)：末班灵异公交·误上车 / 深夜邮局·误投信件 / 老宅借宿·撞上守夜
- **B 总部体系视角（7）**
  - B1 接线与备案(3)：总部接线员·首日值班 / 新人负责人·首次立案 / 备案档案员·编号整理
  - B2 灾害支援(2)：封锁线人员·城市灾害支援 / 实验室协助·样本看护
  - B3 高危档案(2)：队长计划外围·档案整理 / 鬼湖封锁线·外围警戒
- **C 民间驭鬼者视角（10）**
  - C1 早期民间势力(4)：小强俱乐部·新人考核 / 严力线·鬼血交易 / 黄岗村·受雇调查 / 鬼棺村·守夜异变
  - C2 鬼镜与灵异物品(2)：鬼镜交易·第二只鬼 / 人皮纸·遗像疑云
  - C3 论坛与朋友圈(4)：灵异论坛·围观叶真 / 朋友圈外围·招揽试探 / 鬼橱交易者·愿望代价 / 中山市·跨城接触
- **D 规则型地点任务链（11）**
  - D1 城市规则地点(4)：凯撒大酒店·入住规则 / 鬼邮局·送信任务 / 灵异公交·乘车规则 / 坟场送信·墓园路线
  - D2 古宅与县城(4)：古宅守夜·送葬旧规 / 王家摆钟古宅·身份错位 / 太平古镇·招魂人 / 八音盒诅咒·续命疑云
  - D3 高危水域与画境(3)：鬼湖边缘·涉水禁忌 / 鬼画桃源·画中异常 / 梦魇世界·入梦风险
- **E 后期与番外（12）**
  - E1 国际终局(5)：幽灵船登陆前·沿海戒备 / 国王组织·跨境冲突 / 白水镇·诡异连线 / 秦老落幕·终局前夜 / 终幕失衡·多地红色警报
  - E2 葬礼后日谈(4)：葬礼后·档案复核 / 失踪者回访·余波调查 / 灵异退潮·旧址清理 / 第四代与阴阳路·旧时代回声
  - E3 番外线(3)：叶真番外·福利院源头 / 纸人番外·后日谈 / 招魂番外·终局回声
- **F 自定义（1）**：自定义时间点

字段来源：阶段/压力/可见情报/禁剧透边界对应 `全书剧情簇锚点清单.txt` 各簇；后期/番外节点 boundary 收紧，禁止把终局命运、源头位置、关键生路写进早期可见层。

## v10.2 开局剧情选择参考发现（2026-06-02 重新解析 v10.2.png 实测更新）
- 直接解码 `v10.2.png` 的 `chara`/`ccv3` tEXt 块：卡名 `蛊真人 v10.2`，18 个正则脚本，708 个世界书条目；首条消息是 82 字的轻量 `<start>...</start>`。
- 开局选择逻辑在正则脚本 **#0**（脚本名「蛊真人·水墨欢迎页·v5.4锚点可滚动修复」，匹配 `<start>`，44101 字符），不是 #1 主 UI 脚本。
- v10.2 数据结构是三级：`const volumesData = [{title:卷, chapters:[{title:章, events:[{name, time, loc}]}]}]`；JS 用 `createComplexVolume()` 生成卷折叠→章折叠→事件的手风琴互斥菜单。
- 事件只有 3 个字段 `name/time/loc`；选事件后把 `time` 写入 `#timeline`、`loc` 写入 `#location` 两个**可见输入框**，并把「卷·章·事件」拼成显示文本。
- v10.2 规模：7 个卷级分组（含 1 个「人物篇（原文+二创）」按角色切入）、21 章、56 个事件节点。
- **与当前神秘复苏欢迎页的核心差异（实测确认）：**
  - 当前 `anchors` 是两级 `{group, items[{name,loc,phase,pressure,intel,boundary}]}`，只有 5 组 17 个事件（含 1 自定义），其中 `阶段3/5：高危事件与规则地点` 一组硬塞了 10 个从城市灾害到终幕的跨度过大事件。
  - 当前字段比 v10.2 更丰富（6 个 vs 3 个），**唯独缺 `time`**。
  - 当前选节点写隐藏 `#mfrs-anchor-value`，提交时展开字段。二次复核 `开局时空锚点联动规则.txt` 后确认：`time/loc` 应保持“节点自带、不手填”，不新增可见时间/地点输入框。
  - 当前表单原有蛊真人残留分区标题：`身份与根骨`、`修为与流派`（本轮已替换）。
- 结论修正：本轮重点不是「设计新字段」（字段已够），而是「补 `time` + 加中间分层 + 节点从 17 扩到 50-70 + 改手风琴三级菜单」，并保持 time/loc 节点自带。
- 不建议把全部章节塞进欢迎页；世界书详细内容继续由索引/阶段/地点/人物条目召回，节点只给入口和边界。
- 特殊能力默认项需收紧：「永久死机驾驭」适合强外挂模式，不适合默认普通开局，否则削弱复苏代价、调查压力和灵异对抗规则。
- 临时解析产物：`v10_2_card_full.json`、`v10_2_start_regex.html`、`v10_2_welcome_regex.txt`（本地对比用，不提交）。

## 全原著补全规划发现
- 用户当前目标已从“补齐事件索引”升级为“补全整个原著内容的世界书剧情层”。参考源限定为项目根目录 `神秘复苏.txt`。
- 本地原著结构已确认：五卷正文，正文标题止于 `第一千五百六十六章 葬礼`，随后为番外《叶真》《纸人》《招魂（上/中/下）》。
- 已有事件索引层覆盖全书主线和番外，但仍需要继续补“可生成剧情”的中间层：人物阶段状态、地点阶段状态、事件余波、势力行动时钟、厉鬼/物品生命周期、玩家偏移处理。
- 参考 `v10.2.png` 的世界书剧情组织方式后，最值得借鉴的是细粒度剧情节点和阶段状态，而不是单纯增加条目数量。
- A-H 已落地为 8 个新增世界书中间层条目：`全原著覆盖矩阵`、`五卷剧情簇细化-全书`、`人物阶段状态-全书核心人物`、`地点阶段状态-全书核心地点`、`事件余波与支线开局-全书`、`势力行动时钟-全书`、`厉鬼与灵异物品生命周期-全书`、`玩家偏移与情报权限-全书`。
- 本轮新增条目已注册进 `src/神秘复苏模拟器/index.yaml` 并通过 `npm run build`；开发版 PNG 的 `chara/ccv3` 元数据已确认包含 8 个新条目名称。
- I 阶段已用 Chrome DevTools Protocol 连接本地 SillyTavern 进行抽样验收：当前角色与主世界书均为 `神秘复苏模拟器`，Tavern Helper 可读取 383 个世界书条目，其中 378 个启用。
- 关键词层抽样结果：早期七中、中期大昌市灾害、鬼邮局规则地点、后期幽灵船、番外后日谈、玩家偏移情报 6 类提示均能命中主要目标条目；早期/中期/鬼邮局提示未误触发番外或幽灵船，后期幽灵船提示未误触发番外。
- 当前实机注意点：SillyTavern 自带 `getWorldInfoPrompt()` dry-run 对 6 条测试提示均返回空串，但 Tavern Helper 直接读取条目和手工关键词匹配正常。这更像当前酒馆世界书注入链/设置层问题，不是 `index.yaml` 注册、构建或 PNG 元数据丢失。
- 后续精修点：`事件余波与支线开局-全书` 未被“灾害后复核”自然说法命中；`事件索引-王家摆钟古宅与邮局终局` 未被“邮局四楼”自然说法命中；`人物阶段状态-全书核心人物` 未被“何银儿最后一次招魂”自然说法命中，当前主要由人物条目和番外索引承接。
- 进一步排查结论：`getWorldInfoPrompt()` 的空串不是世界书完全未触发，而是因为项目条目大多使用“指定深度”插入，内容不会进入 `worldInfoBefore/worldInfoAfter`；正确观察点是 `checkWorldInfo()` 的 `WIDepthEntries` 和 `allActivatedEntries`。
- 真正的实机问题是 token 预算挤占。以“七中/敲门鬼视频”提示为例，`WORLDINFO_SCAN_DONE` 事件显示扫描阶段找到了 51 个候选条目，包括 `大昌市七中事件`、`敲门鬼`、`大昌市七中`、`敲门鬼事件`、`小剧情锚点-大昌市早期`、`精确锚点-大昌市早期线`、`阶段0开局与七中前后`、`事件索引-大昌市早期` 和 `欢迎页`；但 8192 预算下最终只进入 prompt 的是 `系统提示词`、`对话示例`、`[mvu_update]变量输出格式`、`变量列表` 4 个高顺序常驻条目。
- 预算挤占的主要原因：`index.yaml` 中顺序 14720/14700 的常驻规则和变量条目数量多、内容长，并且都在剧情锚点前面。抽样 token 计数：`系统提示词` 约 2273 tokens、`[mvu_update]变量输出格式` 约 1842、`变量列表` 约 1003、`[mvu_update]变量更新规则` 约 2634；后续规则条目还包括多个 1k-4k tokens 的常驻项。剧情锚点虽匹配，但排在这些常驻内容之后，容易被预算截断。

## 番外补全发现
- 本地 `神秘复苏.txt` 正文后剩余番外已单独拆成 `事件索引-番外叶真纸人与招魂`，覆盖第零章番外《叶真》、第一章番外《纸人》、第二至第四章番外《招魂》。
- 独立番外索引包含五条事件链：叶真福利院源头与阿武记忆处理、贺峰公开禁忌名字与孙瑞/柳三纸人后手、何银儿最后一次招魂、民国亡魂试探杨戬、跨越时间连接张洞与民国众人谢幕。
- `事件索引-白水镇国王组织与终局后日谈` 保留番外总入口，但详细规则交给独立番外索引，避免终局主索引继续膨胀。
- 番外信息默认只用于后日谈、未来线、档案调查或高权限继承者；正文阶段仍禁止直接泄露番外未来情报。

## Active Findings
- 阶段 J 预算精修已执行一轮：大多数高成本 14700 常驻规则已改成绿灯按需触发，`欢迎页` 不再常驻，`[mvu_update]变量更新规则` 和 `对话示例` 不再常驻，`变量输出格式.yaml` 与 `变量列表.txt` 已压缩。
- 实机/CDP 关键结论：原先 8192 预算下只进入 `系统提示词`、`对话示例`、`[mvu_update]变量输出格式`、`变量列表` 的问题已改善；早期七中提示现在能把 `大昌市七中事件`、`敲门鬼事件`、`敲门鬼`、`大昌市七中` 注入最终 depth entries，相关小剧情/精确锚点和事件索引至少进入 activation/candidate 列表。
- 后期幽灵船提示在精修中也能召回 `国王组织`、`幽灵船`、`幽灵船事件`、`阶段7国际冲突与世界失衡`、`事件索引-白水镇国王组织与终局后日谈` 等关键项。
- 仍有残余风险：8192 预算在密集提示下仍可能 overflow；最终一轮 CDP `checkWorldInfo()` 在页面侧偶发超时，但 Tavern 缓存读取、YAML parse、`git diff --check`、`npm run build` 均通过。
- 后续若继续精修，优先考虑两条路：一是把 `系统提示词` 中已被规则文件覆盖的长段落继续压缩；二是把 `变量输出格式` 中示例 JSONPatch 再缩短或改为按需触发，同时保留最低格式契约。
- 原著时间线/事件索引补强第五轮已落地：新增 5 份事件索引，覆盖第六百章以后至本地正文末章第一千五百六十六章《葬礼》附近，并将正文后的番外作为后日谈/特殊开局补充路由。
- 第五轮新增文件：`事件索引-凯撒大酒店与诅咒后续`、`事件索引-鬼邮局前中期与坟场线`、`事件索引-王家摆钟古宅与邮局终局`、`事件索引-鬼湖凯撒二探与鬼画桃源线`、`事件索引-白水镇国王组织与终局后日谈`。
- 本地 `神秘复苏.txt` 未检索到“第一千六百零一章”或“一千六百”标题；正文标题实际止于第一千五百六十六章《葬礼》，后续为番外：第零章番外《叶真》、第一章番外《纸人》、第二至第四章番外《招魂》。
- 第五轮重点路由：凯撒大酒店/八音盒后续、人皮纸活动、鬼梦与熊文文复活、鬼邮局前中期、墓园送信、301室、王家摆钟、大东市古宅、太平古宅守夜、邮局终局、鬼湖、太平古镇/招魂人、梦魇707、凯撒二探、张羡光鬼画桃源、白水镇、国王组织、幽灵船、秦老落幕、第四代、阴阳路、正文后日谈。
- 原著时间线/事件索引补强第四轮已落地：新增 `事件索引-朋友圈清算与八音盒诅咒`，覆盖第五百一十二章《计划确认》至第六百章《离去》附近，作为阶段4“朋友圈公开杀局、八音盒续命、平安大厦清算、方世明正面对抗”的路由层。
- 第四轮重点路由：朋友圈打掉杨间决议、李瑶内线、八音盒获取、鬼画/鬼差牵制、张建/费总清场、鬼剪刀袭击、八音盒续命、厉鬼复苏伪象、平安大厦清算、方世明再现与朋友圈阶段性退场。
- 当前总覆盖口径：按“事件索引”标准，已从早期七中/黄岗村/负责人体系、大昌市灾害、总部显性体系一路补到本地正文末章及番外路由；下一步应做精修和小剧情锚点补强。
- 原著时间线/事件索引补强第二轮已落地：新增 `事件索引-大昌市灾害线`，覆盖第二百一十四章《大型灵异事件》至第二百七十八章《死亡报告》附近，作为阶段3“大昌市城市级灾害”的路由层。
- 第二轮重点路由：饿死鬼事件、鬼婴/鬼童扩散、城市封锁、医院/安全屋、鬼烛行动、王小明方案、队长计划前置、七中源头、棺材钉、五秒计划、战后接收和死亡报告。
- 原著时间线/事件索引补强第一轮已落地为“路由层”：新增两份启用索引负责把早期章节、地点、人物、厉鬼、物品和玩家行为关键词导向既有精确锚点/小剧情锚点；模板文件只作为后续扩写规范，不默认注入世界书。
- 第一轮覆盖范围：大昌市早期（七中、敲门鬼媒介、富仁商场无头鬼影、小强俱乐部、王珊珊异常、人头气球）与黄岗村/负责人体系（黄岗村、鬼棺、鬼镜、冯全、总部备案、负责人体系、王小明实验前置）。
- 发布版 3.0 已完成并推送到 GitHub。推送后远端新增 `422bdc4 [bot] bundle` / tag `v0.0.41`，当前最新远端 `origin/main` 为 `422bdc4`。
- 发布版 `character_version` 已从 `2.0` 改为 `3.0`；开发版仍保持原版本状态，符合“发布版版本改为 3.0”的本次范围。
- 发布版 CDN 当前指向 `c41b53fbe2bd69788cb7ead88c28f6a6a7d7b1ff`，缓存标记为 `phase105-theme-lighten-3-0`。最终发布提交 `a9a1e42` 引用前一个资源提交，是为了让 CDN 能拉到已经存在于仓库中的 dist 文件。
- `npm run build` 已通过，并重新生成发布版 PNG/JSON。
- `<sp_input>` 通用输入面板实机检查通过：桌面与移动端无横向溢出，写入按钮可用，表单区域已比原先更亮。

## EJS/MVU Findings
- `EjsTemplate is not defined` 来自世界书 `变量列表.txt` 使用 `EjsTemplate.allVariables()`，不是 `界面美化/index.ts`。
- 常见触发原因是用户没有完整 Tavern Helper 环境、没有启用对应能力，或 Tavern Helper 版本/API 不提供 `EjsTemplate` 全局。
- MVU 的 `stat_data` 是真实存档变量；EJS 是世界书渲染时的动态展示逻辑。移除 EJS 动态摘要不会删除 MVU 数据。
- 若后续要处理 EJS 兼容，优先方案仍是：
  - 稳妥方案：移除 `变量列表.txt` 中依赖 `EjsTemplate.allVariables()` 的动态摘要，只保留 `{{format_message_variable::stat_data}}` 和字段说明。
  - 折中方案：保留动态摘要，但在 EJS 中增加 `typeof EjsTemplate !== 'undefined'` 兜底。

## Theme Findings
- 默认文章样式过暗主要来自 `界面美化` 全局恐怖主题叠加酒馆主题。
- 方案 A 已完成：直接调浅当前主题的背景、正文、引用、代码块、选项面板、输入控件和阴影。
- 若用户或真实环境仍反馈偏暗，下一步不是继续整体提亮，而是考虑方案 B：把“功能增强”和“暗黑皮肤”拆开，或提供低对比主题开关。

## Commit Boundaries
- 不应提交本地截图、备份、规划文件、`.claude/`、`v10.2.png`、原著文本、参考 JSON 等未跟踪材料。
- 当前未跟踪文件保留在工作区，未纳入发布提交。
- Git 的 `C:\Users\linlang/.config/git/ignore` 权限警告是环境问题，目前不影响正常提交和推送。
- 本地 `main` 当前落后远端自动 bundle 提交 1 个提交；后续改代码前先 pull/rebase，避免基于旧 HEAD 继续发布。
