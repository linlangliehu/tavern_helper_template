# 魔禁卡开发经验沉淀

> 本文档沉淀「魔法禁书目录模拟器」角色卡开发中积累的关键经验与教训。
> 配套文档：`魔禁开发交接文档.md`（当前状态）、持久记忆 #19-#25（agent 跨会话自动携带）。
> 沉淀日期：2026-08-27（卡片首次完整实机验收通过）

---

## 一、工具链关键事实（避坑必读）

### 1.1 tavern_sync 打包器的字段能力边界

tavern_sync 打包器（`tavern_sync.mjs`）只支持 **8 个顶层 YAML 字段**映射到 chara：
```
头像 → avatar          版本 → version        作者 → creator
备注 → creator_notes  第一条消息 → first_messages
角色描述 → description  世界书名称 → worldbook   扩展字段 → extensions
```

**不支持** `system_prompts` / `mes_example` / `post_history_instructions` / `depth_prompt` 顶层字段——这些被 `ignored_fields` 强制设为空字符串。

**影响**：系统提示词、对话示例文件**不能通过顶层字段进 chara**，必须作为**世界书条目**（蓝灯常驻 + 指定深度0 + 角色:系统）注入 prompt。这是魔禁卡为什么把所有规则全放世界书的根本原因。

**验证方法**：`chara.data.system_prompts` / `mes_example` 为空是**设计如此**，不是 bug。排查 AI 不输出协议块时，不要怀疑这两个字段——它们本就该空。

### 1.2 schema.json 由 webpack 自动生成（schema_dump）

`schema.json` 不是手维护的——webpack 构建时由 `schema_dump` 机制从 `schema.ts` 自动生成。

**正确流程**：改 `schema.ts`（真源）→ 跑 `npm run build` → schema.json 自动同步。**不要手改 schema.json**（会被下次构建覆盖，虽无害但多余）。

### 1.3 TavernHelper 4.9.3+ 的脚本执行模型

TavernHelper 给每个 `TH-script` 卡片脚本运行在**独立 iframe + 独立 ES module 作用域**。

**关键限制**：`const` / `let` / `import` 定义不跨脚本条目共享。如果脚本加载器条目只定义 `const loadModule`，其他条目调用 `loadModule(...)` 会全部 `ReferenceError`。

**正确写法**：把 `loadModule` 定义 + 所有 `await loadModule(...)` 调用**合并进同一条脚本条目**（脚本加载器），删除其余模块条目。

### 1.4 世界书条目插入位置（position 编码）

ST 1.18.0 用 `extensions.position`（数字）决定注入位置，不是顶层 `position` 字符串：
- `extensions.position = 4` = **指定深度 + 角色:系统**（at depth as system）
- `position = "after_char"` / `"before_char"` = 顶层兼容字段，不影响实际注入

**tavern_sync 打包时**：YAML 里写 `插入位置: 指定深度0 / 角色:系统` 会被打包成 `extensions.position=4, depth=0, role=0`。

**排查注入位置时**：查 `extensions.position`（数字），不要只看顶层 `position`。

### 1.5 CDN 多节点缓存差异

jsdelivr CDN 节点缓存刷新速度差异巨大：
| 节点 | 归属 | purge 能否清 |
|---|---|---|
| cdn.jsdelivr.net | jsdelivr 自有 | ✅ purge 后秒级更新 |
| gcore/fastly/quantil | 合作节点 | ✅ purge 后较快更新 |
| **testingcf.jsdelivr.net** | **Cloudflare 镜像** | **❌ purge 管不到**，s-maxage=43200，约 12h 自然过期 |

**国内加速用 testingcf 的代价**：更新 tag 后需等约 12h 才全节点生效。急用时可临时换 gcore 节点（已更新）验证。

---

## 二、核心教训（本次开发踩坑提炼）

### 教训 1：tavern_sync 不支持 system_prompts → 用世界书承载

**踩坑**：最初怀疑"AI 不输出 UpdateVariable 是因为系统提示词没进 prompt"，查 chara 发现 `system_prompts` 为空，以为打包漏了，花大量时间排查。

**真相**：tavern_sync 工具本身不支持映射 `system_prompts`/`mes_example`（`ignored_fields` 强制设空）。系统提示词要求靠**世界书条目**承载——这是魔禁卡"规则全放世界书"设计的根本原因，不是遗漏。

**正确认知**：排查协议输出问题时，先确认 tavern_sync 字段能力边界，不要在"为什么 system_prompts 为空"上浪费时间。

### 教训 2：TavernHelper 脚本跨 iframe 隔离 → 合并条目

**踩坑**：index.yaml 原设计 7 个脚本条目（1 脚本加载器定义 `loadModule` + 6 模块条目各调 `loadModule`）。实机发现所有模块 `ReferenceError: loadModule is not defined`，4 卡片不渲染。一度误诊为"`tavern_events` 未桥接"。

**真相**：TavernHelper 4.9.3+ 每个脚本条目运行在独立 iframe + ES module 作用域，`const` 不跨条目共享。

**修复**：7 条合并为 1 条（加载器内含 `const loadModule` 定义 + 6 次串行 `await` 调用）。

**教训**：脚本跨条目共享定义要警惕 ES module 作用域隔离。诊断时先做"决定性对照实验"（在目标 iframe 内手动 import 验证脚本本身正确），再下根因结论——避免被"未桥接"等表面现象误导。

### 教训 3：preset 强制结尾盖过 UpdateVariable → 让位 disclaimer

**踩坑**：能力卡渲染成功但 AI 不输出 UpdateVariable，卡片数据不动态更新。一度怀疑 AI 模型/preset 问题。

**真相**：preset「泉此方方案」强制要求"每次最后输出 `<disclaimer>`"，与我们的「变量输出格式」"结尾必须输出 `<UpdateVariable>`"**直接冲突**。preset 的"最后"指令更明确，盖过我们的 UpdateVariable 要求。

**关键认知**：preset 强制结尾是为解决 gemini-3.7-flash "以 model 标签结尾就没输出"的问题——**不能动 preset 结尾结构**。

**修复**：改「变量输出格式」措辞，把"结尾必须输出 UpdateVariable"改成"**在 disclaimer 之前输出** UpdateVariable"，让位 disclaimer。保留 CRITICAL OVERRIDE 强制力。

**验证支撑**：mvu-protocol-applier 解析 UpdateVariable **位置无关**（正则 `[\s\S]*?` 匹配任意位置块），不要求在结尾，所以位置让位完全可行。

### 教训 4：UpdateVariable 要精简为增量 patch

**踩坑**：即使 AI 输出了 UpdateVariable，也常被截断（输出到第 7 个 patch 就断，后面紧跟 `<StatusPlaceHolderImpl/>`）。

**真相**：UpdateVariable 要求 AI 每轮全量 replace 一堆几乎不变的字段（姓名/性别/年龄等），块臃肿（~800 字符），增加截断风险。

**修复**：加反向禁令——"只 patch 本轮实际变化的字段，未变化的不输出，1-5 patches 而非全量 re-print"。

**预期效果**：块从 ~800 降到 ~200-400，截断概率下降。符合 JSONPatch 增量语义（applier 天然支持）。

### 教训 5：applier 兜底机制是比强求 AI 输出更可靠的保障

**发现**：即使 AI 完全没输出 UpdateVariable（gemini-3.7-flash 不稳定），applier 的 `reconstructUpdateVariableFromSummary` 兜底机制仍能从 AI 的 `<progress>`/`<summary_format>` 摘要重建协议块，成功更新 stat_data。实测卡片显示真实剧情数据。

**教训**：不要把"AI 必须输出协议块"当成唯一路径。applier 兜底是预留的容错设计，实测有效。调优措辞让 AI 主动输出 UpdateVariable 是锦上添花，applier 兜底才是底线保障。

### 教训 6：生成截断根因在上游 API，非卡片

**只读诊断**：gemini-3.7-flash 经 custom 中转端点生成时多次截断。
- `extra.api = "custom"`（非官方 Google API）
- `max_tokens = 30000` 远超实际输出（4052/4360）→ 非预算不足
- 无 stop string
- `finish_reason = null`（中转端点未透传）
- **TTFT 波动 4 倍**（18179ms vs 4449ms）
- 生成时长 31s vs 18s

**根因**：上游 API 不稳定（flash 模型静默停止倾向 + custom 中转流式 SSE 偶发断流）。

**影响**：卡片层已兜底（applier 兜底重建），但用户体验受影响（需多次重新生成）。

**修复方向**（非卡片层）：换官方 Gemini API / 换 pro 模型 / 关流式输出。卡片代码无法解决上游稳定性。

---

## 三、能力卡 3 段式重构（本次主要工作）

### 3.1 字段变更（schema 重构）

| 旧字段 | 新字段 | 说明 |
|---|---|---|
| 能力简述 | **能力效果** | 对齐参考"效果"段 |
| 战术分析 | **实战运用** | 对齐参考"战斗+非战斗"双场景 |
| 副作用或代价（能力）| **删除** | 无风险机制，彻底删除 |
| 副作用（物品）| **删除** | 同上 |

### 3.2 能力卡 3 段式渲染结构

```
▸ 能力名称   等级或位阶 · 阵营类型      ← summary 头部
  效果：[能力效果段落]                 ← 段②
  实战运用：[实战运用段落，含战斗+非战斗] ← 段③
  稳定性：稳定/不稳定                  ← 补充小行
```

### 3.3 全链路同步（避免漏改）

字段重构涉及 **4 个层级**，必须全链路同步：
1. **核心字段层**：schema.ts/json + 消息内面板渲染
2. **契约链文档层**：变量更新规则 + 系统提示词 + 对话示例 + 界面美化
3. **lore 层**：世界书物品/角色档案的"限制/��作用"（**剧情设定，保留不删**）
4. **打包层**：webpack 构建 + PNG 打包 + chara 校验

**教训**：不要只改 schema 就以为完成。AI 生成数据靠契约链文档（系统提示词/对话示例），渲染靠脚本——任何一层漏改都会导致数据与显示不匹配。

---

## 四、关键诊断方法

### 4.1 只读实机验证工具链

| 工具 | 用途 |
|---|---|
| chrome-devtools MCP（连 :9225）| evaluate_script 查 DOM、iframe、window 状态 |
| sillytavern MCP（tavern-tanuki）| get_chat 读原始 AI 回复（未清洗版）、get_character 查 chara |
| node + PNG chunk 解析 | 校验打包产物 chara 内容（字段/世界书条目）|
| D:/Nodejs/node.exe | 处理中文路径，WSL bash 无法连 github |

### 4.2 诊断分层（避免被表象误导）

按 `sillytavern-runtime-debug` skill 的"第一个因果错误"原则：
1. **artifact 层**（卡片渲染）→ 2. **渲染层**（脚本执行）→ 3. **连接器层**（API 端点）→ 4. **API 层**（模型/中转）

每层独立取证，不要因一层的表象跳过其他层。本次"tavern_events 未桥接"误诊就是因为过早下结论，没在目标 iframe 做决定性实验。

### 4.3 决定性对照实验

诊断脚本执行问题时，在目标 iframe 内手动 `await import(CDN URL)` 验证脚本本身正确——能渲染就证明脚本对、问题在注入机制；不能就证明脚本/CDN 有问题。这比从主页面猜更可靠。

---

## 五、修复后验证清单

每次修复后必做的验证闭环：
1. **源码校验**：改的文件内容正确（读回确认）
2. **webpack 构建验证**：`npm run build` 6 脚本 compiled successfully
3. **PNG chara 深度校验**：node 解析 PNG，确认字段/世界书条目进 PNG
4. **实机导入验收**：chrome-devtools 连 9225，查 AI 回复 + 卡片 DOM + stat_data

**关键**：chara 校验要查 **data.character_book**（世界书在顶层，不在 extensions.world——那是世界书名称字符串）。

---

## 六、记忆与文档索引

### 持久记忆（agent 跨会话自动携带）
| ID | 类别 | 内容 |
|---|---|---|
| #19 | ARCHITECTURE | 能力档案字段最终版 + 3 段式渲染 |
| #20 | CONSTRAINTS | testingcf CDN 缓存陷阱（12h 自然过期）|
| #21 | ARCHITECTURE | schema.json 由 webpack schema_dump 自动生成 |
| #22 | CONSTRAINTS | tavern_sync 不支持 system_prompts/mes_example |
| #23 | CONSTRAINTS | TavernHelper 脚本跨 iframe 隔离 |
| #24 | CONSTRAINTS | UpdateVariable 与 preset 冲突 + applier 兜底 |
| #25 | CONSTRAINTS | 生成截断根因在上游 API |

### 配套文档
- `魔禁开发交接文档.md`：当前卡片状态与待办
- 本文档：经验教训沉淀

---

## 七、本次开发全流程回顾（时间线）

1. **删副作用 + 能力卡 3 段式重构**（8 文件 + webpack + 打包 + CDN 推送）
2. **CDN 缓存等待**（testingcf 12h 自然刷新）
3. **架构 bug 修复**（loadModule 跨 iframe 隔离 → 合并脚本条目）
4. **实机验收**（4 卡片渲染成功，但 AI 不输出 UpdateVariable）
5. **根因排查**（tavern_sync 不支持 system_prompts → 世界书位置）
6. **方案 A：让位 disclaimer**（改「变量输出格式」措辞）
7. **方案 A 验收**（AI 仍不输出，但 applier 兜底成功更新 stat_data）
8. **方向 2：精简增量 patch**（加反向禁令减少截断）
9. **截断根因诊断**（上游 API 不稳定，非卡片问题）
10. **经验沉淀**（本文档 + 记忆 #22-#25）

**核心成果**：4 卡片 3 段式渲染正常 + stat_data 动态更新（applier 兜底保障）+ 卡片层已尽力，剩余截断是上游 API 稳定性问题。
