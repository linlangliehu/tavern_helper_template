node_modules

.DS_Store
Thumbs.db

*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

auto-imports.d.ts
components.d.ts

.vscode/chrome-debug-profile/

# 本地调试截图与 a11y 快照
sillytavern_*.png
snapshot_after_sync.txt

# CDP 验证用 Chrome user-data 目录（曾误入版本控制，818 文件）
.tmp-chrome-*/

# 误输入命令 / 误入版本控制的废弃产物
--.json

# 根目录一次性调试截图（theme/SP 输入截图，曾误入版本控制）
/mfrs_sp_input_*.png
/mfrs_theme_*.png

# 大体积原文备忘（《神秘复苏》小说原文，非项目资产，曾误入版本控制）
/神秘复苏.txt

# Claude Code 临时下载（会话产生）
.claude_tmp_*

# 误输入命令产生的文件
et --hard

# 本地 Claude 私有设置（按约定不应 tracked）
.claude/settings.local.json

# Claude Code worktree 工作目录（会话产生，曾误入版本控制）
.claude/worktrees/

# AI 工具本地依赖
.kilo/node_modules/
.kilocode/node_modules/

# 本地 build 的示例产物（CI 会 rm 掉）
/dist/前端界面示例/
/dist/流式楼层界面示例/
/dist/脚本示例/
/dist/角色卡示例/

# 本地构建 sourcemap
/dist/**/*.map

# 审计临时研究文件（会话产生，审计完成后可删）
.tmp-research/

# AI 会话产生的一次性探针脚本与输出（probe/check/diag 等，非项目资产）
.agent-artifacts/

# 本地实时开发会话锁 / 派生开发卡 / 临时导出（PROJECT-FLOW-FIX）
.local/

# 用户本地截图
屏幕截图*.png
2026-*.png
current-vscode-browser.png

# 本地 BGM 冗余拷贝（正式源在 linlangliehu/bgm 仓库 raw 直链）
*.mp3

# pi 本地会话状态
.pi/
